# Reference values for the Wind Evidence Studio local stack.
# This file is not auto-loaded by scripts/start-local-demo.sh; export overrides explicitly.
WORKSPACE_ROOT=.riff-workspaces
MESA_PORT=8091
# Keep this paired with MESA_PORT when overriding either value.
MESA_SERVICE_URL=http://127.0.0.1:8091
PORT=8787
WEB_PORT=5173
# Keep this paired with PORT when overriding either value.
VITE_API_BASE_URL=http://127.0.0.1:8787
