import { createHash } from "node:crypto";
import { closeSync, existsSync, fsyncSync, lstatSync, mkdirSync, openSync, readFileSync, readdirSync, realpathSync, renameSync, unlinkSync, writeFileSync } from "node:fs";
import { join, resolve } from "node:path";
import { ApiError } from "./errors.ts";
import { CANONICAL_JSON_V2, canonicalJsonV2, contentId, sha256Hex } from "./canonical-json-v2.ts";
import type { Attestation, ProjectCommand, VerifiedMesaArtifact, WindModelContract } from "./durable-project-types.ts";
import type { Gate2Runtime } from "./gate2-runtime.ts";
import type { MesaAdapter, WindArtifact } from "./mesa-adapter.ts";
import { GATE3_CANONICAL, type BrowserProjectState, type BrowserProjectionResponse, type FramedCandidateDescriptor, type RuntimeCandidateHandshake, type SourceDescriptor } from "./gate3-types.ts";
import { FRAMED_BUNDLE_FILES, verifyFramedWindBundle } from "./framed-wind-contract.ts";

const HEX = /^[0-9a-f]{64}$/u;
const MODEL_ID = "wind-turbine-maintenance" as const;
const VIEW_FILES = ["model-spec.json", "parameter-schema.json", "execution-field-schema.json", "metric-schema.json", "visualization.json", "traceability.json"] as const;
const BINDING_FILES = ["defaults/wind-turbine-maintenance-demo-v1.json", "provenance.json"] as const;
const ARTIFACT_NAMES = ["request.json", "metadata.json", "daily-kpis.csv", "domain-events.jsonl", "summary.json", "replay-manifest.json", "derived-views-manifest.json", "run.log"] as const;
const REPLAY_IDENTITY_FIELDS = ["project_id", "run_id", "model_id", "model_revision_id", "brief_revision_id", "alignment_revision_id", "experiment_revision_id", "preset_id", "seed", "visibility", "trust_label", "workflow_label", "policy_snapshot_digest", "run_admission_digest"] as const;
const LEGACY_IDENTITY_FIELDS = ["project_id", "run_id", "model_id", "model_revision_id", "experiment_revision_id", "preset_id", "seed"] as const;
const CLAIM_LABELS = ["synthetic_inputs", "single_seed", "behavioral_reproduction_not_runtime_equivalence", "draft_unverified", "no_staffing_recommendation"] as const;
const NON_CLAIMS = ["not_anylogic_runtime_or_numerical_equivalence", "not_calibrated_to_a_real_wind_farm", "single_seed_is_not_uncertainty_analysis", "no_staffing_recommendation"] as const;
const RAW_EVENT_FIELDS = ["event_id", "sequence", "sim_time_days", "event_type", "phase", "turbine_id", "crew_id", "work_order_id", "correlation_id", "before_state", "after_state", "payload"] as const;
const EVENT_PHASES: Readonly<Record<string, readonly number[]>> = { failure_occurred: [10], maintenance_due: [10], request_queued: [10], request_superseded: [10], request_suppressed: [10], repair_completed: [20], maintenance_completed: [20], replacement_completed: [20], crew_return_started: [20], crew_arrived: [30, 40], repair_started: [30, 40], maintenance_started: [20, 30, 40], replacement_started: [30, 40], crew_returned: [30], crew_dispatched: [40], daily_snapshot: [50] };
const STATE_VOCABULARY = new Set(["operating", "failed_waiting", "corrective_repair", "planned_maintenance", "major_replacement", "idle", "driving_to_work", "working", "driving_home"]);
type ArtifactName = typeof ARTIFACT_NAMES[number];
type Plain = Record<string, unknown>;
export type Gate3ActivationCheckpoint = "after_authorization" | "after_candidate_persisted" | "after_project_commit" | "after_mesa_switch" | "after_reconcile" | "after_ready_terminal_event" | "after_ready_reconcile_record" | "after_ready_command_receipt" | "before_ready_client_outcome" | "after_failure_terminal_event" | "after_failure_record" | "after_failure_command_receipt" | "before_failure_client_outcome";
export class Gate3CrashInterruption extends Error { readonly checkpoint: Gate3ActivationCheckpoint; constructor(checkpoint: Gate3ActivationCheckpoint) { super(`simulated Gate 3 interruption at ${checkpoint}`); this.checkpoint = checkpoint; } }

const plain = (value: unknown): value is Plain => value !== null && typeof value === "object" && !Array.isArray(value) && [Object.prototype, null].includes(Object.getPrototypeOf(value));
const exactKeys = (value: unknown, keys: readonly string[], code = "mesa_run_corrupt"): asserts value is Plain => {
  if (!plain(value) || Object.keys(value).sort().join("\n") !== [...keys].sort().join("\n")) throw new ApiError(500, code, "A verified evidence document has an unexpected schema.");
};
const digestWithout = (prefix: string, value: Plain, key: string): string => { const copy = { ...value }; delete copy[key]; return `${prefix}${sha256Hex(canonicalJsonV2(copy))}`; };
const jsonArtifact = (artifact: WindArtifact, maximum: number, canonicalLf = false, canonicalNoLf = false): Plain => {
  if (artifact.bytes.byteLength > maximum) throw new ApiError(500, "mesa_run_corrupt", "A committed JSON artifact exceeds its verified bound.");
  const payload = canonicalLf ? artifact.bytes.subarray(0, -1) : artifact.bytes; if (canonicalLf && artifact.bytes.at(-1) !== 0x0a) throw new ApiError(500, "mesa_run_corrupt", "A framed JSON artifact lacks its exact final LF."); let value: unknown; try { value = JSON.parse(payload.toString("utf8")); } catch { throw new ApiError(500, "mesa_run_corrupt", "A committed JSON artifact is invalid."); }
  if (!plain(value) || (canonicalLf || canonicalNoLf) && !canonicalJsonV2(value).equals(payload)) throw new ApiError(500, "mesa_run_corrupt", "A committed JSON artifact is invalid or non-canonical."); return value;
};
const safeRecordIdentity = (response: Plain): Record<string, string | number | boolean | null> => {
  const result: Record<string, string | number | boolean | null> = {};
  const allowed = new Set(["snapshot_revision", "decision_brief_revision_id", "alignment_map_revision_id", "model_revision_id", "experiment_revision_id", "issue_id", "attestation_batch_id", "run_id", "status", "activation_id"]);
  const visit = (value: unknown): void => { if (!plain(value)) return; for (const [key, item] of Object.entries(value)) { if (allowed.has(key) && (item === null || ["string", "number", "boolean"].includes(typeof item))) result[key] = item as string | number | boolean | null; else if (plain(item)) visit(item); } };
  visit(response); return result;
};
const safeText = (value: unknown): string => String(value ?? "").replace(/[\u0000-\u0008\u000b\u000c\u000e-\u001f\u007f]/gu, "").slice(0, 4_000);

export class Gate3Runtime {
  readonly #gate2: Gate2Runtime;
  readonly #mesa: MesaAdapter;
  readonly #workspaceRoot: string;
  readonly #candidateCache = new Map<string, FramedCandidateDescriptor>();
  readonly #candidateRuntimeInstances = new Map<string, string>();
  readonly #activationReservations = new Map<string, string>();
  readonly #artifactSetCache = new Map<string, Promise<Map<ArtifactName, WindArtifact>>>();
  readonly #eventCache = new Map<string, { events: Plain[]; offsets: number[]; semanticSha256: string }>();
  readonly #fault?: (checkpoint: Gate3ActivationCheckpoint) => void;

  constructor(gate2: Gate2Runtime, mesa: MesaAdapter, workspaceRoot: string, fault?: (checkpoint: Gate3ActivationCheckpoint) => void) { this.#gate2 = gate2; this.#mesa = mesa; this.#workspaceRoot = existsSync(workspaceRoot) ? realpathSync(workspaceRoot) : resolve(workspaceRoot); this.#fault = fault; this.#gate2.store.setRunAdmissionGuard((projectId, modelRevisionId) => this.#assertRunAdmission(projectId, modelRevisionId)); this.#gate2.store.setExperimentMutationGuard((projectId, modelRevisionId) => this.#verifiedActiveExperimentContract(projectId, modelRevisionId)); }

  async recover(): Promise<void> { for (const projectId of this.#gate2.store.projectIds()) await this.#recoverProject(projectId); }

  defaultProject(): Plain {
    const ids = this.#gate2.store.projectIds();
    if (ids.length === 0) throw new ApiError(404, "no_default_project", "No configured durable project is available.");
    if (ids.length !== 1) throw new ApiError(409, "multiple_default_projects", "More than one configured durable project is available.");
    const projectId = ids[0]; const snapshot = this.#gate2.store.snapshot(projectId); const actors = this.#gate2.store.actors(projectId);
    return { schema_id: "riff://evidence-studio/default-project-discovery/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, display_name: snapshot.display_name, health: "healthy_configured", actors: actors.slice(0, 50).map((actor) => ({ actor_id: actor.actor_id, display_name: actor.display_name, actor_type: actor.actor_type, declared_role: actor.declared_role, assurance: "declared_unauthenticated_local" })), actor_count: actors.length, actors_truncated: actors.length > 50 };
  }

  browserProjection(projectId: string): BrowserProjectionResponse {
    const snapshot = this.#gate2.store.snapshot(projectId); const legacy = this.#gate2.store.publicProjection(projectId) as any;
    const brief = snapshot.current.decision_brief_revision_id ? this.#gate2.store.decisionBrief(projectId, snapshot.current.decision_brief_revision_id) : null;
    const alignment = snapshot.current.alignment_map_revision_id ? this.#gate2.store.alignmentMap(projectId, snapshot.current.alignment_map_revision_id) : null;
    const experiment = snapshot.current.experiment_revision_id ? this.#gate2.store.experimentRevision(projectId, snapshot.current.experiment_revision_id) : null;
    let modelView: BrowserProjectState["current_records"]["model_view"] = null;
    if (snapshot.current.model_revision_id) { try { const sources = this.modelViewSources(projectId, snapshot.current.model_revision_id); modelView = { model_id: MODEL_ID, model_revision_id: snapshot.current.model_revision_id, view_sources_href: `/api/projects/${encodeURIComponent(projectId)}/models/${encodeURIComponent(snapshot.current.model_revision_id)}/view-sources`, source_set_digest: String(sources.source_set_digest) }; } catch (error) { if (!(error instanceof ApiError) || ![404, 409].includes(error.status)) throw error; } }
    const events = this.#gate2.store.projectEvents(projectId, Math.max(-1, snapshot.snapshot_revision - 50), 50);
    const recent = events.filter((event) => event.initiator === "client").slice(-25).map((event) => ({ command_id: event.command_id, command_digest: event.command_digest, command_digest_version: (event.event_type.startsWith("model.activation_") ? "gate3-command-digest-v2" : "gate2-command-digest-v1") as "gate2-command-digest-v1" | "gate3-command-digest-v2", event_type: event.event_type, committed_snapshot_revision: event.snapshot_revision, result_identity: safeRecordIdentity(event.response_projection) }));
    const run = snapshot.run_index.find((item) => item.run_id === snapshot.current.run_id); const evidence = run?.reference_kind === "terminal" && run.status === "succeeded" ? this.#gate2.cachedRunEvidence(projectId, run.run_id) : null;
    const terminalItems = evidence?.terminal_metadata ? this.#terminalArtifacts(evidence.terminal_metadata) : []; const terminal = terminalItems.map((item) => ({ artifact_id: item.artifact_id, logical_name: item.name, sha256: item.sha256, href: `/api/projects/${encodeURIComponent(projectId)}/artifacts/${encodeURIComponent(item.artifact_id)}` })).sort((a, b) => a.logical_name.localeCompare(b.logical_name));
    const actors = legacy.actors as any[]; const issues = legacy.issues as any[]; const human = legacy.review_summaries.human; const agent = legacy.review_summaries.agent;
    const activation = this.#activationProjection(projectId); if (activation?.receipt) recent.push({ command_id: String(activation.receipt.command_id), command_digest: String(activation.receipt.command_digest), command_digest_version: "gate3-command-digest-v2", event_type: String(activation.receipt.event_type), committed_snapshot_revision: Number(activation.receipt.committed_snapshot_revision), result_identity: { activation_id: String(activation.receipt.activation_id), terminal_status: String(activation.receipt.terminal_status) } });
    const unsigned: BrowserProjectState = { schema_id: "riff://evidence-studio/project-state/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, display_name: snapshot.display_name, snapshot_revision: snapshot.snapshot_revision, projection_digest: "", phase: snapshot.phase, current: structuredClone(snapshot.current), model_activation: activation?.projection ?? null, current_records: { decision_brief: brief, alignment_map: alignment, model_view: modelView, experiment }, actors: actors.map((actor) => ({ actor_id: actor.actor_id, display_name: actor.display_name, actor_type: actor.actor_type, declared_role: actor.declared_role, assurance: "declared_unauthenticated_local" })), issues, review_summaries: { human, agent }, workflow_policy: legacy.workflow_policy, runs: legacy.runs, current_terminal_artifacts: terminal, recent_command_results: recent.slice(-25), projection_truncation: { actors: { count: Number(legacy.actor_count), truncated: Boolean(legacy.actors_truncated) }, issues: { count: Number(legacy.issue_count), truncated: Number(legacy.issue_count) > issues.length }, human_reviews: { count: Number(human.count), truncated: Boolean(human.truncated) }, agent_reviews: { count: Number(agent.count), truncated: Boolean(agent.truncated) }, runs: { count: Number(legacy.run_count), truncated: Number(legacy.run_count) > legacy.runs.length }, command_results: { count: events.filter((event) => event.initiator === "client").length + (activation?.receipt ? 1 : 0), truncated: snapshot.snapshot_revision > events.length } } };
    const projectionDigest = digestWithout("pd_", unsigned as unknown as Plain, "projection_digest"); const projection = { ...unsigned, projection_digest: projectionDigest };
    return { schema_id: "riff://evidence-studio/browser-projection-response/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, snapshot_revision: snapshot.snapshot_revision, projection_digest: projectionDigest, projection };
  }

  businessRevision(projectId: string, kind: "decision_brief" | "alignment_map", revisionId: string): Plain {
    const record = kind === "decision_brief" ? this.#gate2.store.decisionBrief(projectId, revisionId) : this.#gate2.store.alignmentMap(projectId, revisionId);
    const bytes = canonicalJsonV2(record); const digest = kind === "decision_brief" ? record.decision_brief_digest ?? record.decision_brief_revision_id : record.alignment_digest ?? record.alignment_map_revision_id; const briefId = kind === "decision_brief" ? revisionId : record.brief_revision_id ?? record.decision_brief_revision_id;
    return { schema_id: "riff://evidence-studio/business-revision-response/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, revision_kind: kind, revision_id: revisionId, revision_digest: digest, source: { schema_id: "riff://evidence-studio/source-descriptor/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, source_kind: "business_revision", logical_name: kind, sha256: sha256Hex(Buffer.concat([bytes, Buffer.from("\n")])), identity: { project_id: projectId, model_revision_id: kind === "alignment_map" ? record.model_revision_id : null, brief_revision_id: briefId, alignment_revision_id: kind === "alignment_map" ? revisionId : null, experiment_revision_id: null, run_id: null }, href: kind === "decision_brief" ? `/api/projects/${encodeURIComponent(projectId)}/brief/revisions/${encodeURIComponent(revisionId)}` : `/api/projects/${encodeURIComponent(projectId)}/alignment/revisions/${encodeURIComponent(revisionId)}` }, record };
  }

  attestationPage(projectId: string, subjectId: string, after: string | null, limit: number): Plain {
    if (!/^(?:amr|er)_[0-9a-f]{64}$/u.test(subjectId) || !Number.isSafeInteger(limit) || limit < 1 || limit > 100) throw new ApiError(422, "invalid_request", "Attestation pagination is invalid.");
    const snapshot = this.#gate2.store.snapshot(projectId); if (![snapshot.current.alignment_map_revision_id, snapshot.current.experiment_revision_id].includes(subjectId)) throw new ApiError(404, "resource_not_found", "The requested resource was not found.");
    const all = this.#gate2.store.attestations(projectId).filter((item) => item.subject_revision_id === subjectId).sort((a, b) => a.created_at.localeCompare(b.created_at) || a.attestation_id.localeCompare(b.attestation_id));
    let start = 0; if (after) { const decoded = this.#decodeCursor(after, projectId, subjectId); start = all.findIndex((item) => item.created_at === decoded.created_at && item.attestation_id === decoded.attestation_id) + 1; if (start === 0) throw new ApiError(422, "invalid_request", "The attestation cursor is invalid."); }
    const selected = all.slice(start, start + limit); const heads = new Map<string, Attestation>(); const supersededBy = new Map<string, string>();
    for (const item of all) { const key = `${item.actor_id}:${item.scope}`; if (item.supersedes_attestation_id) supersededBy.set(item.supersedes_attestation_id, item.attestation_id); heads.set(key, item); }
    const issues = new Map(snapshot.issue_index.map((item) => [item.issue_id, item]));
    const items = selected.map((item) => { const actor = this.#gate2.store.actor(projectId, item.actor_id); return { attestation_id: item.attestation_id, actor: { actor_id: actor.actor_id, display_name: actor.display_name, actor_type: actor.actor_type, declared_role: actor.declared_role, assurance: "declared_unauthenticated_local" }, subject_revision_ids: [item.subject_revision_id], scope: item.scope, decision: item.decision, rationale: safeText(item.rationale), issue_refs: item.issue_ids.map((issueId) => { const issue = issues.get(issueId); if (!issue) throw new ApiError(500, "project_corrupt", "An attestation references an unreachable issue."); return { issue_id: issue.issue_id, title: safeText(this.#gate2.store.issueHistory(projectId, issueId).events[0].payload.title), status: issue.status, href: `/api/projects/${encodeURIComponent(projectId)}/issues/${encodeURIComponent(issueId)}/history` }; }), created_at: item.created_at, supersedes_attestation_id: item.supersedes_attestation_id, superseded_by_attestation_id: supersededBy.get(item.attestation_id) ?? null, effective_head: heads.get(`${item.actor_id}:${item.scope}`)?.attestation_id === item.attestation_id, record_digest: item.attestation_digest }; });
    const hasMore = start + selected.length < all.length; const last = selected.at(-1); const next = hasMore && last ? this.#encodeCursor(projectId, subjectId, last.created_at, last.attestation_id) : null;
    return { schema_id: "riff://evidence-studio/attestation-detail-page/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, subject_revision_id: subjectId, after, next_after: next, has_more: hasMore, items };
  }

  async framedCandidate(projectId: string): Promise<FramedCandidateDescriptor> {
    this.#gate2.store.snapshot(projectId);
    if (!this.#mesa.getWindRuntimeCandidateHandshake) throw new ApiError(503, "mesa_runtime_handshake_unavailable", "The Mesa runtime handshake is unavailable.");
    const handshake = await this.#mesa.getWindRuntimeCandidateHandshake(projectId); this.#verifyHandshake(projectId, handshake);
    const source = this.#loadCandidateSourceDescriptor();
    if (source.candidate_source_revision !== handshake.candidate_source_revision || source.manifest_sha256 !== handshake.candidate_manifest_sha256 || source.file_map_sha256 !== handshake.candidate_file_map_sha256 || source.source_descriptor_digest !== handshake.candidate_source_descriptor_digest) throw new ApiError(409, "framed_candidate_source_mismatch", "The framed candidate source does not match the reviewed descriptor.");
    if (!handshake.active_model_revision_id) throw new ApiError(409, "active_model_mismatch", "The project has no active model to activate from.");
    const unsigned: FramedCandidateDescriptor = { schema_id: "riff://evidence-studio/framed-candidate-descriptor/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, runtime_handshake_digest: handshake.handshake_digest, expected_active_model_revision_id: handshake.active_model_revision_id, candidate_source_revision: handshake.candidate_source_revision, model_id: MODEL_ID, model_revision_id: source.model_revision_id, bundle_protocol: "wind-turbine-maintenance-bundle-v2-framed", manifest_sha256: source.manifest_sha256, file_map_sha256: source.file_map_sha256, runtime_profile: { canonical_json_version: GATE3_CANONICAL, mesa_version: "3.5.1", model_protocol_version: "wind-turbine-maintenance-v2-framed-replay", python_implementation: "CPython", python_major_minor: "3.12" }, preset_id: "wind-turbine-maintenance-demo-v1", preset_sha256: source.preset_sha256, provenance_sha256: source.provenance_sha256, descriptor_digest: "" };
    const descriptor = { ...unsigned, descriptor_digest: digestWithout("cand_", unsigned as unknown as Plain, "descriptor_digest") }; this.#candidateCache.set(projectId, descriptor); this.#candidateRuntimeInstances.set(projectId, handshake.runtime_instance_id); return structuredClone(descriptor);
  }

  async activate(command: ProjectCommand<{ source_model_revision_id: string; source_brief_revision_id: string; source_alignment_map_revision_id: string; source_experiment_revision_id: string; expected_candidate_descriptor_digest: string }>): Promise<{ status: number; body: Plain }> {
    exactKeys(command, ["command_id", "project_id", "session_id", "base_snapshot_revision", "payload"], "invalid_request"); exactKeys(command.payload, ["source_model_revision_id", "source_brief_revision_id", "source_alignment_map_revision_id", "source_experiment_revision_id", "expected_candidate_descriptor_digest"], "invalid_request");
    const route = `/api/projects/${encodeURIComponent(command.project_id)}/wind/framed-evidence/activate`; const commandDigest = `cmd_${sha256Hex(canonicalJsonV2({ version: "gate3-command-digest-v2", method: "POST", actual_normalized_route: route, request: command }))}`; const activationDir = join(this.#workspaceRoot, "projects", command.project_id, "activations", command.command_id); const outcomePath = join(activationDir, "client-outcome.json"); const reservationPath = join(activationDir, "command-reservation.json");
    if (existsSync(outcomePath)) return this.#completeTerminal(command.project_id, command.command_id, commandDigest);
    if (existsSync(reservationPath)) {
      if (this.#terminalEvent(command.project_id, command.command_id)) return this.#completeTerminal(command.project_id, command.command_id, commandDigest);
      return this.#replayReservation(this.#readJsonFile(reservationPath), command.project_id, command.command_id, commandDigest);
    }
    const preflight = this.#gate2.store.gate3ActorContext(command); const descriptor = await this.framedCandidate(command.project_id); const observedActivation = this.#authoritativeActivation(command.project_id); if (this.#activationReservations.has(command.project_id) || observedActivation && observedActivation.status !== "ready") throw new ApiError(409, "concurrent_activation", "Another model activation is in progress."); const { snapshot, actor } = this.#gate2.store.gate3CommandContext(command); const source = { model_revision_id: command.payload.source_model_revision_id, brief_revision_id: command.payload.source_brief_revision_id, alignment_revision_id: command.payload.source_alignment_map_revision_id, experiment_revision_id: command.payload.source_experiment_revision_id };
    if (actor.actor_id !== preflight.actor.actor_id || actor.actor_type !== "human" || actor.declared_role !== "project_owner") throw new ApiError(403, "actor_permission_denied", "Only an attached human project_owner may activate a model revision."); if (snapshot.run_index.some((run) => run.reference_kind === "pending")) throw new ApiError(409, "active_run_conflict", "A model cannot be activated while a run is active."); if (canonicalJsonV2(source).toString() !== canonicalJsonV2({ model_revision_id: snapshot.current.model_revision_id, brief_revision_id: snapshot.current.decision_brief_revision_id, alignment_revision_id: snapshot.current.alignment_map_revision_id, experiment_revision_id: snapshot.current.experiment_revision_id }).toString()) throw new ApiError(409, "revision_not_current", "The activation source tuple is not current.");
    if (descriptor.descriptor_digest !== command.payload.expected_candidate_descriptor_digest || descriptor.expected_active_model_revision_id !== source.model_revision_id || this.#candidateRuntimeInstance(command.project_id) === "") throw new ApiError(409, "stale_candidate", "The framed candidate descriptor is stale.");
    if (!this.#mesa.materializeFramedCandidate || !this.#mesa.captureFramedCandidate || !this.#mesa.captureFramedCandidateBytes || !this.#mesa.casFramedActiveModel || !this.#mesa.getFramedActivationStatus) throw new ApiError(503, "mesa_adapter_failure", "The Mesa activation protocol is unavailable.");
    const currentActivation = this.#authoritativeActivation(command.project_id); if (this.#activationReservations.has(command.project_id) || currentActivation && currentActivation.status !== "ready") throw new ApiError(409, "concurrent_activation", "Another model activation is in progress.");
    const now = new Date().toISOString(); const target = this.#buildActivationTarget(command.project_id, actor.actor_id, now, source, descriptor); this.#gate2.store.registerModelContract(target.contract);
    const staged = [target.brief, target.alignment, target.experiment].map((item) => this.#stagedRef(command.project_id, actor.actor_id, now, item.kind, item.id, item.value)); const manifestUnsigned: Plain = { schema_id: "riff://evidence-studio/activation-staging-manifest/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, created_at: now, created_by_actor_id: actor.actor_id, source, target: target.tuple, brief: { ...staged[0], parent_brief_revision_id: source.brief_revision_id, copy_rule: "exact_content_activation_copy_v1" }, alignment: { ...staged[1], parent_alignment_revision_id: source.alignment_revision_id, brief_revision_id: target.tuple.brief_revision_id, model_revision_id: target.tuple.model_revision_id, migration_rule: "framed_alignment_rebind_v1" }, experiment: { ...staged[2], parent_experiment_revision_id: null, model_revision_id: target.tuple.model_revision_id, brief_revision_id: target.tuple.brief_revision_id, alignment_revision_id: target.tuple.alignment_revision_id, preset_id: "wind-turbine-maintenance-demo-v1", copy_migration_rule: "framed_parameter_copy_revalidate_v1", defaults_digest: target.experiment.value.defaults_digest, parameter_defaults_digest: `pv_${sha256Hex(canonicalJsonV2(target.experiment.value.parameter_defaults))}`, parameters_digest: `pv_${sha256Hex(canonicalJsonV2(target.experiment.value.parameters))}`, parameter_diff_digest: `pv_${sha256Hex(canonicalJsonV2(target.experiment.value.parameter_diff))}`, execution_defaults_digest: `ev_${sha256Hex(canonicalJsonV2(target.experiment.value.execution_defaults))}`, execution_values_digest: `ev_${sha256Hex(canonicalJsonV2(target.experiment.value.execution_values))}`, execution_diff_digest: `ev_${sha256Hex(canonicalJsonV2(target.experiment.value.execution_diff))}` }, staging_manifest_digest: "" }; const stagingManifest = { ...manifestUnsigned, staging_manifest_digest: digestWithout("astage_", manifestUnsigned, "staging_manifest_digest") };
    const intentUnsigned: Plain = { schema_id: "riff://evidence-studio/activation-intent/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, command_digest_version: "gate3-command-digest-v2", base_snapshot_revision: snapshot.snapshot_revision, expected_event_head_digest: snapshot.previous_event_digest, authorized_actor_id: actor.actor_id, source, planned_target: target.tuple, staging_manifest_digest: stagingManifest.staging_manifest_digest, staged_record_refs: { brief: staged[0], alignment: staged[1], experiment: staged[2] }, expected_candidate_descriptor_digest: descriptor.descriptor_digest, runtime_handshake_digest: descriptor.runtime_handshake_digest, runtime_instance_id: this.#candidateRuntimeInstance(command.project_id), candidate_source_revision: descriptor.candidate_source_revision, expected_active_model_revision_id: descriptor.expected_active_model_revision_id, created_at: now, intent_digest: "" }; const intent = { ...intentUnsigned, intent_digest: digestWithout("aint_", intentUnsigned, "intent_digest") };
    const pendingBody = { schema_id: "riff://evidence-studio/activation-command-pending/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, status: "authorizing", snapshot_revision: snapshot.snapshot_revision }; const reservationUnsigned: Plain = { schema_id: "riff://evidence-studio/activation-command-reservation/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: command.project_id, activation_id: command.command_id, command_id: command.command_id, command_digest: commandDigest, http_status: 202, http_body: pendingBody, created_at: now, reservation_digest: "" }; const reservation = { ...reservationUnsigned, reservation_digest: digestWithout("ares_", reservationUnsigned, "reservation_digest") };
    const stageRecords = [
      { relative: `activation-staging/${command.command_id}/brief.json`, value: target.brief.value, ref: { kind: "decision_brief_revision", id: target.brief.id, digest: target.brief.digest }, event_ref: false },
      { relative: `activation-staging/${command.command_id}/alignment.json`, value: target.alignment.value, ref: { kind: "alignment_map_revision", id: target.alignment.id, digest: target.alignment.digest }, event_ref: false },
      { relative: `activation-staging/${command.command_id}/experiment.json`, value: target.experiment.value, ref: { kind: "experiment_revision", id: target.experiment.id, digest: target.experiment.digest }, event_ref: false },
      { relative: `activation-staging/${command.command_id}/staging-manifest.json`, value: stagingManifest, ref: { kind: "activation_staging_manifest", id: command.command_id, digest: String(stagingManifest.staging_manifest_digest) } },
      { relative: `activations/${command.command_id}/intent.json`, value: intent, ref: { kind: "activation_intent", id: command.command_id, digest: String(intent.intent_digest) } },
      { relative: `activations/${command.command_id}/command-reservation.json`, value: reservation, ref: { kind: "activation_command_reservation", id: command.command_id, digest: String(reservation.reservation_digest) }, event_ref: false },
    ];
    this.#activationReservations.set(command.project_id, command.command_id); try { this.#gate2.store.gate3SystemTransition({ project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, event_type: "model.activation_authorized", records: stageRecords, response: { activation_id: command.command_id, status: "authorizing" } }); } catch (error) { this.#activationReservations.delete(command.project_id); throw error; }
    this.#fault?.("after_authorization");
    try {
      const candidate = await this.#mesa.materializeFramedCandidate({ schema_id: "riff://mesa-wind/materialize-candidate-request/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, expected_old_model_revision_id: source.model_revision_id, candidate_descriptor_digest: descriptor.descriptor_digest, intent_digest: String(intent.intent_digest) }); const capture = await this.#mesa.captureFramedCandidate(command.command_id); this.#verifyCandidateCapture(command.project_id, command.command_id, descriptor, intent, candidate, capture);
      const capturedDigest = String(capture.candidate_bytes_digest); const candidateReceiptDigest = String((capture.candidate_receipt as Plain).candidate_receipt_digest); const byteCapture = await this.#mesa.captureFramedCandidateBytes(command.project_id, command.command_id, candidateReceiptDigest); const capturedFiles = this.#verifyCandidateByteCapture(command.project_id, command.command_id, descriptor, candidateReceiptDigest, capturedDigest, byteCapture.document, byteCapture.wire); for (const [name, bytes] of capturedFiles) this.#gate2.store.writeGate3CapturedBytes(command.project_id, command.command_id, descriptor.model_revision_id, name, bytes); const observationAgain = await this.#mesa.captureFramedCandidate(command.command_id); this.#verifyCandidateCapture(command.project_id, command.command_id, descriptor, intent, candidate, observationAgain); if (observationAgain.candidate_bytes_digest !== capturedDigest) throw new ApiError(409, "candidate_bytes_changed", "The candidate changed after byte capture."); this.#fault?.("after_candidate_persisted"); const beforeCommit = this.#gate2.store.snapshot(command.project_id); const bindingUnsigned: Plain = { schema_id: "riff://evidence-studio/activation-target-binding/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, source, target: target.tuple, base_snapshot_revision: beforeCommit.snapshot_revision, base_project_event_digest: beforeCommit.previous_event_digest, intent_digest: intent.intent_digest, staging_manifest_digest: stagingManifest.staging_manifest_digest, staged_record_refs: { brief: staged[0], alignment: staged[1], experiment: staged[2] }, candidate_receipt_digest: candidateReceiptDigest, captured_candidate_bytes_digest: capturedDigest, target_binding_digest: "" }; const binding = { ...bindingUnsigned, target_binding_digest: digestWithout("atb_", bindingUnsigned, "target_binding_digest") };
      const commitEvent = this.#gate2.store.gate3SystemTransition({ project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, event_type: "model.activation_committed", records: [
        { relative: `activations/${command.command_id}/target-binding.json`, value: binding, ref: { kind: "activation_target_binding", id: command.command_id, digest: String(binding.target_binding_digest) } },
        { relative: `alignment/decision-brief/revisions/${target.brief.id}/revision.json`, value: target.brief.value, ref: { kind: "decision_brief_revision", id: target.brief.id, digest: target.brief.digest } },
        { relative: `alignment/requirement-map/revisions/${target.alignment.id}/revision.json`, value: target.alignment.value, ref: { kind: "alignment_map_revision", id: target.alignment.id, digest: target.alignment.digest } },
        { relative: `experiments/revisions/${target.experiment.id}/experiment.json`, value: target.experiment.value, ref: { kind: "experiment_revision", id: target.experiment.id, digest: target.experiment.digest } },
      ], phase: "review", current: { decision_brief_revision_id: target.tuple.brief_revision_id, alignment_map_revision_id: target.tuple.alignment_revision_id, model_revision_id: target.tuple.model_revision_id, experiment_revision_id: target.tuple.experiment_revision_id, run_id: null }, response: { activation_id: command.command_id, status: "mesa_switch_pending" } });
      this.#fault?.("after_project_commit"); const casRequest = { schema_id: "riff://mesa-wind/active-cas-request/v1", schema_version: 1 as const, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, expected_old_model_revision_id: source.model_revision_id, target_model_revision_id: target.tuple.model_revision_id, candidate_receipt_digest: candidateReceiptDigest, project_event_digest: commitEvent.event_digest }; const switched = this.#verifySwitchReceipt(await this.#mesa.casFramedActiveModel(casRequest), casRequest); this.#fault?.("after_mesa_switch"); const status = this.#verifyActivationStatus(await this.#mesa.getFramedActivationStatus(command.command_id), casRequest, switched);
      const markerUnsigned: Plain = { schema_id: "riff://evidence-studio/activation-reconcile-marker/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: command.command_id, project_id: command.project_id, target_binding_digest: binding.target_binding_digest, base_project_event_digest: commitEvent.event_digest, base_snapshot_revision: commitEvent.snapshot_revision, switch_receipt_digest: switched.switch_receipt_digest, verified_project_target_model_revision_id: target.tuple.model_revision_id, verified_mesa_active_model_revision_id: status.active_model_revision_id, reconciled_at: new Date().toISOString(), reconcile_digest: "" }; const marker = { ...markerUnsigned, reconcile_digest: digestWithout("arec_", markerUnsigned, "reconcile_digest") }; const reconcileEvent = this.#gate2.store.gate3SystemTransition({ project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, event_type: "model.activation_reconciled", records: [{ relative: `activations/${command.command_id}/reconcile.json`, value: marker, ref: { kind: "activation_reconcile_marker", id: command.command_id, digest: String(marker.reconcile_digest) } }], response: { activation_id: command.command_id, status: "ready" } });
      this.#fault?.("after_reconcile"); this.#fault?.("after_ready_terminal_event"); this.#fault?.("after_ready_reconcile_record"); const result = this.#completeTerminal(command.project_id, command.command_id, commandDigest, true); this.#activationReservations.delete(command.project_id); return result;
    } catch (error) {
      if (error instanceof Gate3CrashInterruption) throw error;
      const safe = error instanceof ApiError ? error : new ApiError(500, "mesa_adapter_failure", "The model activation failed."); const committed = this.#gate2.store.projectEventByCommand(command.project_id, command.command_id, "model.activation_committed"); let terminalStatus: "failed_no_effect" | "failed_fenced" = "failed_fenced";
      if (!committed) { try { const fresh = this.#gate2.store.snapshot(command.project_id); const handshake = await this.#mesa.getWindRuntimeCandidateHandshake?.(command.project_id); if (handshake) { this.#verifyHandshake(command.project_id, handshake); if (fresh.current.model_revision_id === source.model_revision_id && handshake.active_model_revision_id === source.model_revision_id) terminalStatus = "failed_no_effect"; } } catch { /* Unprovable no-effect remains fail-closed. */ } }
      const correlationId = crypto.randomUUID(); const failedAt = new Date().toISOString();
      this.#gate2.store.gate3SystemTransition({ project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, event_type: "model.activation_failed", records: [], response: { activation_id: command.command_id, status: terminalStatus, safe_error_code: safe.code, safe_error_message: safe.message, correlation_id: correlationId, failed_at: failedAt } });
      this.#fault?.("after_failure_terminal_event"); const result = this.#completeTerminal(command.project_id, command.command_id, commandDigest, true); this.#activationReservations.delete(command.project_id); return result;
    }
  }

  async #recoverProject(projectId: string): Promise<void> {
    const root = join(this.#workspaceRoot, "projects", projectId, "activations"); if (!existsSync(root)) return;
    const ids = readdirSync(root).filter((name) => /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/iu.test(name));
    for (const activationId of ids) { this.#activationReservations.set(projectId, activationId); try { const terminal = this.#terminalEvent(projectId, activationId); if (terminal) this.#completeTerminal(projectId, activationId, String(this.#readJsonFile(join(root, activationId, "intent.json")).command_digest)); else await this.#recoverActivation(projectId, activationId); } catch (error) { if (!this.#recoverableActivationError(error)) this.#recordRecoveryFailure(projectId, activationId, error); } finally { this.#activationReservations.delete(projectId); } }
  }

  async #recoverActivation(projectId: string, activationId: string): Promise<void> {
    if (!this.#mesa.materializeFramedCandidate || !this.#mesa.captureFramedCandidate || !this.#mesa.captureFramedCandidateBytes || !this.#mesa.casFramedActiveModel || !this.#mesa.getFramedActivationStatus) throw new ApiError(503, "mesa_adapter_failure", "The Mesa activation protocol is unavailable.");
    const directory = join(this.#workspaceRoot, "projects", projectId, "activations", activationId); const intent = this.#readJsonFile(join(directory, "intent.json"));
    if (intent.schema_id !== "riff://evidence-studio/activation-intent/v1" || intent.project_id !== projectId || intent.activation_id !== activationId || intent.command_id !== activationId || digestWithout("aint_", intent, "intent_digest") !== intent.intent_digest || !plain(intent.source) || !plain(intent.planned_target) || typeof intent.command_digest !== "string") throw new ApiError(500, "immutable_record_corrupt", "The activation intent is corrupt.");
    const authorized = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_authorized"); if (!authorized) throw new ApiError(500, "immutable_record_corrupt", "The activation intent has no committed authorization event.");
    const stagingRoot = join(this.#workspaceRoot, "projects", projectId, "activation-staging", activationId); const manifest = this.#readJsonFile(join(stagingRoot, "staging-manifest.json")); if (manifest.activation_id !== activationId || manifest.project_id !== projectId || manifest.staging_manifest_digest !== intent.staging_manifest_digest || digestWithout("astage_", manifest, "staging_manifest_digest") !== manifest.staging_manifest_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation staging manifest is corrupt.");
    const source = intent.source as { model_revision_id: string; brief_revision_id: string; alignment_revision_id: string; experiment_revision_id: string }; const installed = this.#loadCandidateSourceDescriptor(); const descriptorUnsigned: FramedCandidateDescriptor = { schema_id: "riff://evidence-studio/framed-candidate-descriptor/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, runtime_handshake_digest: String(intent.runtime_handshake_digest), expected_active_model_revision_id: String(intent.expected_active_model_revision_id), candidate_source_revision: String(intent.candidate_source_revision), model_id: MODEL_ID, model_revision_id: installed.model_revision_id, bundle_protocol: "wind-turbine-maintenance-bundle-v2-framed", manifest_sha256: installed.manifest_sha256, file_map_sha256: installed.file_map_sha256, runtime_profile: { canonical_json_version: GATE3_CANONICAL, mesa_version: "3.5.1", model_protocol_version: "wind-turbine-maintenance-v2-framed-replay", python_implementation: "CPython", python_major_minor: "3.12" }, preset_id: "wind-turbine-maintenance-demo-v1", preset_sha256: installed.preset_sha256, provenance_sha256: installed.provenance_sha256, descriptor_digest: "" }; const descriptor = { ...descriptorUnsigned, descriptor_digest: digestWithout("cand_", descriptorUnsigned as unknown as Plain, "descriptor_digest") };
    if (descriptor.descriptor_digest !== intent.expected_candidate_descriptor_digest || descriptor.model_revision_id !== (intent.planned_target as Plain).model_revision_id || descriptor.expected_active_model_revision_id !== source.model_revision_id) throw new ApiError(500, "immutable_record_corrupt", "The frozen activation descriptor no longer matches its intent.");
    const stagedBrief = this.#readJsonFile(join(stagingRoot, "brief.json")); const stagedAlignment = this.#readJsonFile(join(stagingRoot, "alignment.json")); const stagedExperiment = this.#readJsonFile(join(stagingRoot, "experiment.json")); const currentModel = this.#gate2.store.snapshot(projectId).current.model_revision_id; let target: any;
    if (currentModel === source.model_revision_id) { target = this.#buildActivationTarget(projectId, String(intent.authorized_actor_id), String(intent.created_at), source, descriptor); if (![canonicalJsonV2(stagedBrief).equals(canonicalJsonV2(target.brief.value)), canonicalJsonV2(stagedAlignment).equals(canonicalJsonV2(target.alignment.value)), canonicalJsonV2(stagedExperiment).equals(canonicalJsonV2(target.experiment.value))].every(Boolean)) throw new ApiError(500, "immutable_record_corrupt", "The frozen activation target records differ from their immutable derivation."); }
    else { const tuple = intent.planned_target as Plain; this.#verifyFrozenActivationRevision(stagedBrief, "decision_brief_revision_id", "decision_brief_digest", "dbr_", "dbrd_"); this.#verifyFrozenActivationRevision(stagedAlignment, "alignment_revision_id", "alignment_digest", "amr_", "amd_"); this.#verifyFrozenActivationRevision(stagedExperiment, "experiment_revision_id", "experiment_digest", "er_", "erd_"); const contract = this.#gate2.store.modelContract(projectId, String(tuple.model_revision_id)); target = { contract, tuple, brief: { kind: "decision_brief", id: stagedBrief.decision_brief_revision_id, digest: stagedBrief.decision_brief_digest, value: stagedBrief }, alignment: { kind: "alignment_map", id: stagedAlignment.alignment_revision_id, digest: stagedAlignment.alignment_digest, value: stagedAlignment }, experiment: { kind: "experiment_revision", id: stagedExperiment.experiment_revision_id, digest: stagedExperiment.experiment_digest, value: stagedExperiment } }; }
    if (!canonicalJsonV2(target.tuple).equals(canonicalJsonV2(intent.planned_target))) throw new ApiError(500, "immutable_record_corrupt", "The frozen activation tuple differs from its intent."); this.#gate2.store.registerModelContract(target.contract);
    const staged = [target.brief, target.alignment, target.experiment].map((item) => this.#stagedRef(projectId, String(intent.authorized_actor_id), String(intent.created_at), item.kind, item.id, item.value)); const commitEvent = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_committed"); let binding: Plain; let committed = commitEvent;
    if (!committed) {
      const candidate = await this.#mesa.materializeFramedCandidate({ schema_id: "riff://mesa-wind/materialize-candidate-request/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, project_id: projectId, expected_old_model_revision_id: source.model_revision_id, candidate_descriptor_digest: descriptor.descriptor_digest, intent_digest: String(intent.intent_digest) }); const capture = await this.#mesa.captureFramedCandidate(activationId); this.#verifyCandidateCapture(projectId, activationId, descriptor, intent, candidate, capture); const bytesDigest = String(capture.candidate_bytes_digest); const receiptDigest = String((capture.candidate_receipt as Plain).candidate_receipt_digest); const byteCapture = await this.#mesa.captureFramedCandidateBytes(projectId, activationId, receiptDigest); const files = this.#verifyCandidateByteCapture(projectId, activationId, descriptor, receiptDigest, bytesDigest, byteCapture.document, byteCapture.wire); for (const [name, bytes] of files) this.#gate2.store.writeGate3CapturedBytes(projectId, activationId, descriptor.model_revision_id, name, bytes); const observed = await this.#mesa.captureFramedCandidate(activationId); this.#verifyCandidateCapture(projectId, activationId, descriptor, intent, candidate, observed); if (observed.candidate_bytes_digest !== bytesDigest) throw new ApiError(409, "candidate_bytes_changed", "The candidate changed after byte capture.");
      const base = this.#gate2.store.snapshot(projectId); const unsigned: Plain = { schema_id: "riff://evidence-studio/activation-target-binding/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, project_id: projectId, source, target: target.tuple, base_snapshot_revision: base.snapshot_revision, base_project_event_digest: base.previous_event_digest, intent_digest: intent.intent_digest, staging_manifest_digest: manifest.staging_manifest_digest, staged_record_refs: { brief: staged[0], alignment: staged[1], experiment: staged[2] }, candidate_receipt_digest: receiptDigest, captured_candidate_bytes_digest: bytesDigest, target_binding_digest: "" }; binding = { ...unsigned, target_binding_digest: digestWithout("atb_", unsigned, "target_binding_digest") };
      committed = this.#gate2.store.gate3SystemTransition({ project_id: projectId, command_id: activationId, command_digest: String(intent.command_digest), event_type: "model.activation_committed", records: [{ relative: `activations/${activationId}/target-binding.json`, value: binding, ref: { kind: "activation_target_binding", id: activationId, digest: String(binding.target_binding_digest) } }, { relative: `alignment/decision-brief/revisions/${target.brief.id}/revision.json`, value: target.brief.value, ref: { kind: "decision_brief_revision", id: target.brief.id, digest: target.brief.digest } }, { relative: `alignment/requirement-map/revisions/${target.alignment.id}/revision.json`, value: target.alignment.value, ref: { kind: "alignment_map_revision", id: target.alignment.id, digest: target.alignment.digest } }, { relative: `experiments/revisions/${target.experiment.id}/experiment.json`, value: target.experiment.value, ref: { kind: "experiment_revision", id: target.experiment.id, digest: target.experiment.digest } }], phase: "review", current: { decision_brief_revision_id: target.tuple.brief_revision_id, alignment_map_revision_id: target.tuple.alignment_revision_id, model_revision_id: target.tuple.model_revision_id, experiment_revision_id: target.tuple.experiment_revision_id, run_id: null }, response: { activation_id: activationId, status: "mesa_switch_pending" } });
    } else { binding = this.#readJsonFile(join(directory, "target-binding.json")); if (binding.target_binding_digest !== digestWithout("atb_", binding, "target_binding_digest") || binding.intent_digest !== intent.intent_digest || !canonicalJsonV2(binding.target).equals(canonicalJsonV2(target.tuple))) throw new ApiError(500, "immutable_record_corrupt", "The activation target binding is corrupt."); }
    const reconcilePath = join(directory, "reconcile.json"); let reconcileEvent = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_reconciled"); let marker: Plain;
    if (!reconcileEvent) { const casRequest = { schema_id: "riff://mesa-wind/active-cas-request/v1", schema_version: 1 as const, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, project_id: projectId, expected_old_model_revision_id: source.model_revision_id, target_model_revision_id: String(target.tuple.model_revision_id), candidate_receipt_digest: String(binding.candidate_receipt_digest), project_event_digest: committed.event_digest }; const switched = this.#verifySwitchReceipt(await this.#mesa.casFramedActiveModel(casRequest), casRequest); const status = this.#verifyActivationStatus(await this.#mesa.getFramedActivationStatus(activationId), casRequest, switched); const unsigned: Plain = { schema_id: "riff://evidence-studio/activation-reconcile-marker/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, project_id: projectId, target_binding_digest: binding.target_binding_digest, base_project_event_digest: committed.event_digest, base_snapshot_revision: committed.snapshot_revision, switch_receipt_digest: switched.switch_receipt_digest, verified_project_target_model_revision_id: target.tuple.model_revision_id, verified_mesa_active_model_revision_id: status.active_model_revision_id, reconciled_at: new Date().toISOString(), reconcile_digest: "" }; marker = { ...unsigned, reconcile_digest: digestWithout("arec_", unsigned, "reconcile_digest") }; reconcileEvent = this.#gate2.store.gate3SystemTransition({ project_id: projectId, command_id: activationId, command_digest: String(intent.command_digest), event_type: "model.activation_reconciled", records: [{ relative: `activations/${activationId}/reconcile.json`, value: marker, ref: { kind: "activation_reconcile_marker", id: activationId, digest: String(marker.reconcile_digest) } }], response: { activation_id: activationId, status: "ready" } }); } else { marker = this.#readJsonFile(reconcilePath); this.#verifyReconcileMarker(marker, projectId, activationId, binding, committed); }
    this.#completeTerminal(projectId, activationId, String(intent.command_digest));
  }

  #recoverableActivationError(error: unknown): boolean { return error instanceof ApiError && [502, 503, 504].includes(error.status) && !["immutable_record_corrupt", "project_corrupt", "candidate_bytes_changed"].includes(error.code); }
  #recordRecoveryFailure(projectId: string, activationId: string, error: unknown): void {
    const directory = join(this.#workspaceRoot, "projects", projectId, "activations", activationId); let intent: Plain; try { intent = this.#readJsonFile(join(directory, "intent.json")); } catch { return; }
    if (!this.#terminalEvent(projectId, activationId)) { const safe = error instanceof ApiError ? error : new ApiError(500, "activation_recovery_failed", "Activation recovery found an unrecoverable mismatch."); const terminalStatus: "failed_no_effect" | "failed_fenced" = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_committed") ? "failed_fenced" : "failed_no_effect"; const correlationId = crypto.randomUUID(); const failedAt = new Date().toISOString(); this.#gate2.store.gate3SystemTransition({ project_id: projectId, command_id: activationId, command_digest: String(intent.command_digest), event_type: "model.activation_failed", records: [], response: { activation_id: activationId, status: terminalStatus, safe_error_code: safe.code, safe_error_message: safe.message, correlation_id: correlationId, failed_at: failedAt } }); }
    this.#completeTerminal(projectId, activationId, String(intent.command_digest));
  }

  modelViewSources(projectId: string, modelRevisionId: string): Plain {
    this.#gate2.store.modelContract(projectId, modelRevisionId); const candidateRoot = this.#bundleRoot(projectId, modelRevisionId); let candidateManifest: unknown; try { candidateManifest = JSON.parse(readFileSync(join(candidateRoot, "manifest.json"), "utf8")); } catch { throw new ApiError(500, "immutable_record_corrupt", "The model bundle manifest is invalid."); } if (!plain(candidateManifest) || candidateManifest.schema_version !== 2) throw new ApiError(409, "view_contract_unavailable", "The historical model revision has no framed view contract."); const verified = verifyFramedWindBundle(this.#workspaceRoot, projectId, { model_revision_id: modelRevisionId }); const root = verified.root; const manifest = verified.manifest;
    const all = [...VIEW_FILES, ...BINDING_FILES]; const descriptors = new Map<string, SourceDescriptor>(); const parsed = new Map<string, Plain>();
    let responseBytes = 0; for (const name of all) { const declaration = (manifest.files as Plain)[name]; const bytes = verified.files.get(name)!; responseBytes += bytes.byteLength; const descriptor: SourceDescriptor = { schema_id: "riff://evidence-studio/source-descriptor/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, source_kind: "model_bundle", logical_name: name, sha256: String(declaration.sha256), identity: { project_id: projectId, model_revision_id: modelRevisionId, brief_revision_id: null, alignment_revision_id: null, experiment_revision_id: null, run_id: null }, href: `/api/projects/${encodeURIComponent(projectId)}/models/${encodeURIComponent(modelRevisionId)}/view-sources/${encodeURIComponent(name)}` }; descriptors.set(name, descriptor); if ((VIEW_FILES as readonly string[]).includes(name)) parsed.set(name, JSON.parse(bytes.subarray(0, -1).toString("utf8"))); }
    if (responseBytes > 512 * 1024) throw new ApiError(500, "immutable_record_corrupt", "The parsed model-view sources exceed their total bound.");
    const sourceMap = Object.fromEntries([...descriptors].map(([name, item]) => [name, item.sha256]).sort(([a], [b]) => a.localeCompare(b))); const sourceSetDigest = `viewsrc_${sha256Hex(canonicalJsonV2(sourceMap))}`;
    this.#verifyFramedParameterContract(parsed.get("parameter-schema.json")!, this.#readJsonBytes(this.#readBundleFile(root, BINDING_FILES[0])));
    const response = { schema_id: "riff://evidence-studio/model-view-sources/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, model_id: MODEL_ID, model_revision_id: modelRevisionId, generator_contract_version: "wind-evidence-view-contract-v1", sources: { model_spec: descriptors.get("model-spec.json"), parameter_schema: descriptors.get("parameter-schema.json"), execution_field_schema: descriptors.get("execution-field-schema.json"), metric_schema: descriptors.get("metric-schema.json"), visualization: descriptors.get("visualization.json"), traceability: descriptors.get("traceability.json") }, contract_bindings: { default_preset: descriptors.get(BINDING_FILES[0]), provenance: descriptors.get(BINDING_FILES[1]) }, model_spec: parsed.get("model-spec.json"), parameter_schema: parsed.get("parameter-schema.json"), execution_field_schema: parsed.get("execution-field-schema.json"), metric_schema: parsed.get("metric-schema.json"), visualization: parsed.get("visualization.json"), traceability: parsed.get("traceability.json"), source_set_digest: sourceSetDigest };
    if (canonicalJsonV2(response).byteLength > 512 * 1024) throw new ApiError(500, "immutable_record_corrupt", "The serialized model-view response exceeds its total bound.");
    return response;
  }

  modelViewSource(projectId: string, modelRevisionId: string, name: string): { bytes: Buffer; sha256: string } {
    if (![...VIEW_FILES, ...BINDING_FILES].includes(name as any)) throw new ApiError(404, "resource_not_found", "The requested resource was not found."); const response = this.modelViewSources(projectId, modelRevisionId) as any; const descriptor = Object.values({ ...response.sources, ...response.contract_bindings }).find((item: any) => item.logical_name === name) as any; return { bytes: this.#readBundleFile(this.#bundleRoot(projectId, modelRevisionId), name), sha256: descriptor.sha256 };
  }

  async evidenceIndex(projectId: string, runId: string): Promise<Plain> {
    const run = this.#gate2.store.run(projectId, runId); const evidence = await this.#gate2.verifiedRunEvidence(projectId, runId); const terminal = evidence.terminal_metadata;
    if (run.reference_kind !== "terminal" || run.status !== "succeeded" || !terminal) throw new ApiError(409, "run_evidence_pending", "Run evidence is not complete.");
    const declarations = this.#artifactDeclarations(this.#terminalArtifacts(terminal)); const artifacts = await this.#verifiedArtifactSet(projectId, runId, evidence); const framed = "terminal_metadata_kind" in terminal; const summary = jsonArtifact(artifacts.get("summary.json")!, 128 * 1024, framed); const replay = jsonArtifact(artifacts.get("replay-manifest.json")!, 4 * 1024 * 1024, framed); const derived = jsonArtifact(artifacts.get("derived-views-manifest.json")!, 512 * 1024, framed); const identity = this.#runIdentity(run); const properties = this.#metricProperties(projectId, run);
    const eventArtifact = artifacts.get("domain-events.jsonl")!; const indexedEvents = this.#verifiedEvents(projectId, run, eventArtifact, declarations.get("domain-events.jsonl")!); let claimLabels: string[]; let nonClaims: string[]; const replayVerified = this.#verifyReplayManifest(replay, run, declarations.get("domain-events.jsonl")!, properties); this.#verifyReplayEventBinding(projectId, run, replay, replayVerified, eventArtifact, indexedEvents); claimLabels = replayVerified.claimLabels; nonClaims = replayVerified.nonClaims;
    if (framed) { this.#verifySummary(summary, run, properties); await this.kpis(projectId, runId, -1, 1); const kpiRows = parseCsv(artifacts.get("daily-kpis.csv")!.bytes.toString("utf8")); const kpiHeader = kpiRows[0]; const terminalCells = kpiRows.at(-1)!; const terminalMetrics = Object.fromEntries(Object.keys(properties).map((name) => [name, this.#parseMetricCell(terminalCells[kpiHeader.indexOf(name)], properties[name])])); if (!canonicalJsonV2(summary.metrics).equals(canonicalJsonV2(terminalMetrics))) throw new ApiError(500, "mesa_run_corrupt", "The summary metrics differ from the terminal KPI row."); const experiment = this.#gate2.store.experimentRevision(projectId, run.experiment_revision_id) as any; const measurementDays = Number(experiment.execution_values.horizon_days) - Number(experiment.execution_values.warmup_days); const annualRevenue = Number(terminalMetrics.operating_revenue) * 365 / measurementDays; const annualExpense = Number(terminalMetrics.total_maintenance_cost) * 365 / measurementDays; const close = (actual: unknown, expected: number): boolean => typeof actual === "number" && Number.isFinite(actual) && Math.abs(actual - expected) <= Math.max(1e-9, Math.abs(expected) * 1e-12); if (summary.measurement_window_days !== measurementDays || summary.minimum_availability_fraction !== experiment.parameters.minimum_availability_fraction || summary.minimum_availability_met !== (Number(terminalMetrics.availability_fraction) >= Number(summary.minimum_availability_fraction)) || !close(summary.annualized_operating_revenue, annualRevenue) || !close(summary.annualized_maintenance_expense, annualExpense) || !close(summary.annualized_maintenance_cost, annualExpense) || !close(summary.annualized_profit, annualRevenue - annualExpense)) throw new ApiError(500, "mesa_run_corrupt", "The summary decision and annualized metrics are not independently recomputed.");
      exactKeys(derived, ["schema_id", "schema_version", "canonical_json_version", "manifest_kind", "identity", "generator", "inputs", "claim_labels", "non_claims", "projection_digests"], "mesa_run_corrupt"); this.#assertArtifactIdentity(derived, run, true); this.#labels(derived.claim_labels, CLAIM_LABELS, "claim labels"); this.#labels(derived.non_claims, NON_CLAIMS, "non-claims"); exactKeys(derived.generator, ["generator_id", "generator_version"], "mesa_run_corrupt"); exactKeys(derived.inputs, ["metadata_core", "model_sources", "artifacts"], "mesa_run_corrupt"); exactKeys(derived.projection_digests, ["event_projection_sha256", "kpi_projection_sha256", "replay_projection_sha256", "label_projection_sha256"], "mesa_run_corrupt"); if (derived.schema_id !== "riff://wind-turbine-maintenance/derived-views-manifest/framed/v1" || derived.schema_version !== 1 || derived.canonical_json_version !== GATE3_CANONICAL || derived.manifest_kind !== "framed_evidence_views" || !canonicalJsonV2(derived.generator).equals(canonicalJsonV2({ generator_id: "wind-evidence-derived-views", generator_version: "wind-evidence-derived-views-v1" }))) throw new ApiError(500, "mesa_run_corrupt", "The derived-view manifest discriminator or generator is invalid."); const metadata = jsonArtifact(artifacts.get("metadata.json")!, 512 * 1024, true); exactKeys((derived.inputs as Plain).metadata_core, ["metadata_core_digest"], "mesa_run_corrupt"); if (((derived.inputs as Plain).metadata_core as Plain).metadata_core_digest !== metadata.metadata_core_digest) throw new ApiError(500, "mesa_run_corrupt", "The derived metadata-core DAG edge is invalid."); exactKeys((derived.inputs as Plain).model_sources, ["model_revision_id", "source_set_digest"], "mesa_run_corrupt"); const modelSources = this.modelViewSources(projectId, run.model_revision_id); if (!canonicalJsonV2((derived.inputs as Plain).model_sources).equals(canonicalJsonV2({ model_revision_id: run.model_revision_id, source_set_digest: modelSources.source_set_digest }))) throw new ApiError(500, "mesa_run_corrupt", "The derived model-source DAG edge is invalid."); const inputs = (derived.inputs as Plain).artifacts; if (!plain(inputs) || Object.keys(inputs).sort().join("\n") !== ["request.json", "daily-kpis.csv", "domain-events.jsonl", "summary.json", "replay-manifest.json"].sort().join("\n")) throw new ApiError(500, "mesa_run_corrupt", "The derived-view artifact input set is not exact."); for (const [name, binding] of Object.entries(inputs)) { exactKeys(binding, ["artifact_id", "sha256"], "mesa_run_corrupt"); const declaration = declarations.get(name as ArtifactName); const expectedArtifactId = declaration ? `artifact_${sha256Hex(canonicalJsonV2({ run_id: runId, name, sha256: declaration.sha256 }))}` : null; if (!declaration || declaration.artifact_id !== expectedArtifactId || binding.artifact_id !== declaration.artifact_id || binding.sha256 !== declaration.sha256) throw new ApiError(500, "mesa_run_corrupt", "A derived-view artifact binding differs from terminal metadata or content identity."); } const expectedProjections = { event_projection_sha256: { projection_kind: "filtered_domain_events", projection_schema_version: 1, run_id: runId, domain_events_sha256: declarations.get("domain-events.jsonl")!.sha256, event_count: indexedEvents.events.length }, kpi_projection_sha256: { projection_kind: "daily_kpis", projection_schema_version: 1, run_id: runId, daily_kpis_sha256: declarations.get("daily-kpis.csv")!.sha256, summary_sha256: declarations.get("summary.json")!.sha256 }, replay_projection_sha256: { projection_kind: "sampled_replay", projection_schema_version: 1, run_id: runId, replay_manifest_sha256: declarations.get("replay-manifest.json")!.sha256, manifest_kind: replayVerified.kind, frame_count: replay.frames?.length ?? 0 }, label_projection_sha256: { projection_kind: "run_labels", projection_schema_version: 1, run_id: runId, summary_sha256: declarations.get("summary.json")!.sha256, replay_manifest_sha256: declarations.get("replay-manifest.json")!.sha256, claim_labels: CLAIM_LABELS, non_claims: NON_CLAIMS } }; const expectedDigests = Object.fromEntries(Object.entries(expectedProjections).map(([name, preimage]) => [name, sha256Hex(canonicalJsonV2(preimage))])); if (!canonicalJsonV2(derived.projection_digests).equals(canonicalJsonV2(expectedDigests)) || !canonicalJsonV2(summary.claim_labels).equals(canonicalJsonV2(claimLabels)) || !canonicalJsonV2(summary.non_claims).equals(canonicalJsonV2(nonClaims))) throw new ApiError(500, "mesa_run_corrupt", "The derived projections or evidence label carriers disagree."); }
    else { this.#assertArtifactIdentity(summary, run, false); this.#assertArtifactIdentity(derived, run, false); claimLabels = this.#labels(summary.claim_labels, CLAIM_LABELS, "claim labels"); if (!canonicalJsonV2(claimLabels).equals(canonicalJsonV2(replayVerified.claimLabels))) throw new ApiError(500, "mesa_run_corrupt", "The legacy claim label carriers disagree."); nonClaims = [...NON_CLAIMS]; }
    const sourceLinks = ARTIFACT_NAMES.map((name) => this.#artifactSource(projectId, runId, run, declarations.get(name)!));
    const replaySummary = { schema_id: "riff://evidence-studio/replay-manifest-summary/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, manifest_kind: replayVerified.kind, manifest_sha256: declarations.get("replay-manifest.json")!.sha256, generator_version: framed ? replay.generator_version : null, sampling_algorithm: framed ? replay.sampling_algorithm : null, event_source_sha256: framed ? (replay.event_source as Plain).raw_sha256 : replay.source_sha256, event_semantic_sha256: framed ? (replay.event_source as Plain).semantic_sha256 : replay.canonical_event_sha256, event_count: framed ? (replay.event_source as Plain).event_count : replay.event_count, frame_count: framed ? replay.frame_count : 0, sample_days_sha256: framed ? replay.sample_days_sha256 : null, claim_labels_sha256: sha256Hex(canonicalJsonV2(claimLabels)), non_claims_sha256: framed ? sha256Hex(canonicalJsonV2(nonClaims)) : null, unavailable_reason: replayVerified.kind === "unavailable_population_limit" ? "population_exceeds_frame_contract" : replayVerified.kind === "legacy_frameless" ? "legacy_frameless_manifest" : null };
    return { schema_id: "riff://evidence-studio/run-evidence-index/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, run, identity, labels: { visibility: "private_draft", trust_label: "draft_unverified", workflow_label: run.workflow_label, claim_labels: claimLabels, non_claims: nonClaims }, summary, replay_manifest_summary: replaySummary, derived_views_manifest: derived, artifacts: ARTIFACT_NAMES.map((name) => { const item = declarations.get(name)!; return { artifact_id: item.artifact_id, logical_name: name, sha256: item.sha256, href: `/api/projects/${encodeURIComponent(projectId)}/artifacts/${encodeURIComponent(item.artifact_id)}` }; }), source_links: sourceLinks };
  }

  async kpis(projectId: string, runId: string, afterDay: number, limit: number): Promise<Plain> {
    if (!Number.isSafeInteger(afterDay) || afterDay < -1 || !Number.isSafeInteger(limit) || limit < 1 || limit > 366) throw new ApiError(422, "invalid_request", "KPI pagination is invalid.");
    const run = this.#gate2.store.run(projectId, runId); const evidence = await this.#gate2.verifiedRunEvidence(projectId, runId); if (!evidence.terminal_metadata) throw new ApiError(409, "run_evidence_pending", "Run evidence is not complete."); const declarations = this.#artifactDeclarations(this.#terminalArtifacts(evidence.terminal_metadata)); const artifacts = await this.#verifiedArtifactSet(projectId, runId, evidence); const artifact = artifacts.get("daily-kpis.csv")!; const parsed = parseCsv(artifact.bytes.toString("utf8")); if (parsed.length < 2 || artifact.bytes.at(-1) !== 0x0a) throw new ApiError(500, "mesa_run_corrupt", "The KPI artifact is empty or lacks its final LF.");
    const properties = this.#metricProperties(projectId, run); const header = parsed[0]; const identityFields = header.includes("workflow_label") ? REPLAY_IDENTITY_FIELDS : LEGACY_IDENTITY_FIELDS; const expectedHeader = [...identityFields, ...Object.keys(properties)]; if (header.length !== new Set(header).size || header.join("\n") !== expectedHeader.join("\n")) throw new ApiError(500, "mesa_run_corrupt", "The KPI header does not exactly match run identity and metric schema order.");
    const rows = parsed.slice(1).map((cells) => { const result: Plain = {}; for (let index = 0; index < header.length; index += 1) { const key = header[index]; const text = cells[index]; if (key === "seed") result[key] = this.#parseIntegerCell(text); else if ((identityFields as readonly string[]).includes(key)) result[key] = text; else result[key] = this.#parseMetricCell(text, properties[key]); } return result; });
    for (let index = 0; index < rows.length; index += 1) { const row = rows[index]; const identity = this.#replayIdentity(run); for (const key of identityFields) if (row[key] !== identity[key]) throw new ApiError(500, "mesa_run_corrupt", "A KPI row identity differs from the committed run."); this.#verifyMetricProjection(Object.fromEntries(Object.keys(properties).map((key) => [key, row[key]])), properties); if (row.sim_time_days !== index) throw new ApiError(500, "mesa_run_corrupt", "KPI days are not contiguous from day zero."); }
    const experiment = this.#gate2.store.experimentRevision(projectId, run.experiment_revision_id) as any; if (Number(experiment.execution_values?.horizon_days) + 1 !== rows.length) throw new ApiError(500, "mesa_run_corrupt", "The KPI row count differs from the committed experiment horizon.");
    const selected = rows.filter((row) => Number(row.sim_time_days) > afterDay).slice(0, limit); const next = selected.length ? Number(selected.at(-1)!.sim_time_days) : afterDay;
    let metricSource: SourceDescriptor; try { const view = this.modelViewSources(projectId, run.model_revision_id) as any; metricSource = view.sources.metric_schema; } catch { metricSource = this.#artifactSource(projectId, runId, run, declarations.get("summary.json")!); }
    return { schema_id: "riff://evidence-studio/kpi-page/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, run_id: runId, experiment_revision_id: run.experiment_revision_id, source: this.#artifactSource(projectId, runId, run, declarations.get("daily-kpis.csv")!), metric_schema_source: metricSource, columns: header.map((key) => ({ key, label: key.replaceAll("_", " "), unit: plain(properties[key]) && typeof properties[key].unit === "string" ? properties[key].unit : null })), rows: selected, after_day: afterDay, next_after_day: next, has_more: rows.some((row) => Number(row.sim_time_days) > next) };
  }

  async replay(projectId: string, runId: string, afterFrame: number, limit: number): Promise<Plain> {
    if (!Number.isSafeInteger(afterFrame) || afterFrame < -1 || !Number.isSafeInteger(limit) || limit < 1 || limit > 31) throw new ApiError(422, "invalid_request", "Replay pagination is invalid."); const run = this.#gate2.store.run(projectId, runId); const evidence = await this.#gate2.verifiedRunEvidence(projectId, runId); if (!evidence.terminal_metadata) throw new ApiError(409, "run_evidence_pending", "Run evidence is not complete."); const declarations = this.#artifactDeclarations(this.#terminalArtifacts(evidence.terminal_metadata)); const artifacts = await this.#verifiedArtifactSet(projectId, runId, evidence); const framed = "terminal_metadata_kind" in evidence.terminal_metadata; const manifest = jsonArtifact(artifacts.get("replay-manifest.json")!, 4 * 1024 * 1024, framed); const eventArtifact = artifacts.get("domain-events.jsonl")!; const indexedEvents = this.#verifiedEvents(projectId, run, eventArtifact, declarations.get("domain-events.jsonl")!); const verified = this.#verifyReplayManifest(manifest, run, declarations.get("domain-events.jsonl")!, this.#metricProperties(projectId, run)); this.#verifyReplayEventBinding(projectId, run, manifest, verified, eventArtifact, indexedEvents);
    const frames = verified.frames; const selected = frames.filter((frame) => Number(frame.frame_index) > afterFrame).slice(0, limit); const next = selected.length ? Number(selected.at(-1)!.frame_index) : afterFrame; const kind = verified.kind;
    return { schema_id: "riff://evidence-studio/replay-page/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, page_kind: kind, project_id: projectId, run_id: runId, event_source: this.#artifactSource(projectId, runId, run, declarations.get("domain-events.jsonl")!), manifest_source: this.#artifactSource(projectId, runId, run, declarations.get("replay-manifest.json")!), generator_version: framed ? manifest.generator_version : null, source_set_digest: `replay_${sha256Hex(canonicalJsonV2({ event: declarations.get("domain-events.jsonl")!.sha256, manifest: declarations.get("replay-manifest.json")!.sha256 }))}`, sampling_algorithm: framed ? manifest.sampling_algorithm : null, declared_population: manifest.declared_population ?? { turbine_count: 0, crew_count: 0 }, sample_days: framed ? manifest.sample_days : [], sample_days_sha256: framed ? manifest.sample_days_sha256 : null, frame_count: framed ? manifest.frame_count : 0, unavailable_reason: framed ? manifest.unavailable_reason ?? null : "legacy_frameless_manifest", source_event_ranges: framed && Array.isArray(manifest.source_event_ranges) ? (manifest.source_event_ranges as Plain[]).filter((range) => selected.some((frame) => frame.source_event_range_index === range.range_index)) : [], frames: selected, after_frame: afterFrame, next_after_frame: next, has_more: frames.some((frame) => Number(frame.frame_index) > next) };
  }

  async filteredEvents(projectId: string, runId: string, after: number, limit: number, filters: Record<string, string | number | null>): Promise<Plain> {
    if (!Number.isSafeInteger(after) || after < 0 || !Number.isSafeInteger(limit) || limit < 1 || limit > 500) throw new ApiError(422, "invalid_request", "Event pagination is invalid."); const run = this.#gate2.store.run(projectId, runId); const evidence = await this.#gate2.verifiedRunEvidence(projectId, runId); if (!evidence.terminal_metadata) throw new ApiError(409, "run_evidence_pending", "Run evidence is not complete."); const declarations = this.#artifactDeclarations(this.#terminalArtifacts(evidence.terminal_metadata)); const artifacts = await this.#verifiedArtifactSet(projectId, runId, evidence); const declaration = declarations.get("domain-events.jsonl")!; const scannedPage = this.#scanFilteredEvents(projectId, run, artifacts.get("domain-events.jsonl")!, declaration, after, limit, filters);
    return { schema_id: "riff://evidence-studio/filtered-domain-event-projection-page/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, projection_kind: "filtered_domain_events", project_id: projectId, run_id: runId, experiment_revision_id: run.experiment_revision_id, source: this.#artifactSource(projectId, runId, run, declaration), filters, source_event_count: scannedPage.sourceCount, after, scanned_through_sequence: scannedPage.scanned, next_after: scannedPage.scanned, has_more: scannedPage.scanned < scannedPage.sourceCount, events: scannedPage.events };
  }

  #artifactDeclarations(items: VerifiedMesaArtifact[]): Map<ArtifactName, VerifiedMesaArtifact> { const map = new Map(items.map((item) => [item.name as ArtifactName, item])); if (items.length !== ARTIFACT_NAMES.length || ARTIFACT_NAMES.some((name) => !map.has(name)) || new Set(items.map((item) => item.name)).size !== ARTIFACT_NAMES.length) throw new ApiError(500, "mesa_run_corrupt", "Terminal evidence does not declare the exact eight artifacts."); return map; }
  async #verifiedArtifactSet(projectId: string, runId: string, evidence: import("./durable-project-types.ts").VerifiedMesaRunEvidence): Promise<Map<ArtifactName, WindArtifact>> { const terminal = evidence.terminal_metadata; if (!terminal) throw new ApiError(409, "run_evidence_pending", "Run evidence is not complete."); const declarations = this.#artifactDeclarations(this.#terminalArtifacts(terminal)); const key = `${projectId}:${runId}:${terminal.terminal_metadata_digest}`; let pending = this.#artifactSetCache.get(key); if (!pending) { pending = (async () => { const entries = await Promise.all(ARTIFACT_NAMES.map(async (name) => { const declaration = declarations.get(name)!; const cached = this.#readArtifactCache(declaration); if (cached) return [name, { bytes: cached, filename: name, media_type: name.endsWith(".json") ? "application/json" : "text/plain" }] as const; const artifact = await this.#gate2.artifactByName(projectId, runId, name); if (declaration.byte_length !== undefined && declaration.byte_length !== artifact.bytes.byteLength || sha256Hex(artifact.bytes) !== declaration.sha256) throw new ApiError(500, "mesa_run_corrupt", "A cached artifact differs from terminal metadata."); const bytes = Buffer.from(artifact.bytes); this.#persistArtifactCache(declaration.sha256, bytes); return [name, { ...artifact, bytes }] as const; })); return new Map(entries); })(); this.#artifactSetCache.set(key, pending); pending.catch(() => this.#artifactSetCache.delete(key)); } return await pending; }
  #readArtifactCache(declaration: VerifiedMesaArtifact): Buffer | null { const path = join(this.#derivedCacheRoot("artifact-cache"), declaration.sha256); if (!existsSync(path)) return null; try { const stat = lstatSync(path); if (stat.isSymbolicLink() || !stat.isFile()) throw new Error("unsafe cache entry"); const bytes = readFileSync(path); if (declaration.byte_length !== undefined && bytes.byteLength !== declaration.byte_length || sha256Hex(bytes) !== declaration.sha256) throw new Error("cache drift"); return bytes; } catch { try { unlinkSync(path); } catch {} return null; } }
  #persistArtifactCache(digest: string, bytes: Buffer): void { const root = this.#derivedCacheRoot("artifact-cache"); const target = join(root, digest); if (existsSync(target)) { const stat = lstatSync(target); if (stat.isSymbolicLink() || !stat.isFile() || sha256Hex(readFileSync(target)) !== digest) { try { unlinkSync(target); } catch {} } else return; } const temporary = join(root, `.${digest}.${crypto.randomUUID()}.tmp`); let descriptor: number | null = null; try { descriptor = openSync(temporary, "wx", 0o600); writeFileSync(descriptor, bytes); fsyncSync(descriptor); closeSync(descriptor); descriptor = null; renameSync(temporary, target); const directory = openSync(root, "r"); try { fsyncSync(directory); } finally { closeSync(directory); } } catch { if (descriptor !== null) try { closeSync(descriptor); } catch {} try { unlinkSync(temporary); } catch {} throw new ApiError(503, "derived_cache_unavailable", "The verified artifact cache could not be promoted atomically."); } }
  #derivedCacheRoot(kind: "artifact-cache" | "event-index"): string { let current = this.#workspaceRoot; for (const segment of ["derived-cache", kind]) { current = join(current, segment); if (!existsSync(current)) mkdirSync(current, { mode: 0o700 }); const stat = lstatSync(current); if (stat.isSymbolicLink() || !stat.isDirectory() || !realpathSync(current).startsWith(`${this.#workspaceRoot}/`)) throw new ApiError(503, "derived_cache_unavailable", "The derived evidence cache path is unsafe."); } return realpathSync(current); }
  #parseIntegerCell(value: string): number { if (!/^-?(?:0|[1-9][0-9]*)$/u.test(value)) throw new ApiError(500, "mesa_run_corrupt", "A KPI integer cell is invalid."); const parsed = Number(value); if (!Number.isSafeInteger(parsed)) throw new ApiError(500, "mesa_run_corrupt", "A KPI integer cell is unsafe."); return parsed; }
  #parseMetricCell(value: string, declaration: unknown): number { if (!plain(declaration) || !["integer", "number"].includes(String(declaration.type))) throw new ApiError(500, "mesa_run_corrupt", "A metric declaration has an unsupported type."); const parsed = declaration.type === "integer" ? this.#parseIntegerCell(value) : Number(value); if (typeof parsed !== "number" || !Number.isFinite(parsed) || declaration.type === "number" && value.trim() === "" || Array.isArray(declaration.enum) && !declaration.enum.includes(parsed)) throw new ApiError(500, "mesa_run_corrupt", "A KPI metric cell is invalid."); return parsed; }
  #verifiedEvents(projectId: string, run: any, artifact: WindArtifact, declaration: VerifiedMesaArtifact): { events: Plain[]; offsets: number[]; semanticSha256: string } { const cacheKey = `${declaration.sha256}:${artifact.bytes.byteLength}:event-index-v1`; const cached = this.#eventCache.get(cacheKey); if (cached) return cached; if (declaration.byte_length !== undefined && artifact.bytes.byteLength !== declaration.byte_length || sha256Hex(artifact.bytes) !== declaration.sha256 || artifact.bytes.at(-1) !== 0x0a) throw new ApiError(500, "mesa_run_corrupt", "The event artifact bytes differ from terminal metadata or lack final LF."); const runtimeProfile = this.#gate2.store.modelContract(projectId, run.model_revision_id).runtime_profile; const semantic = createHash("sha256"); const events: Plain[] = []; const offsets: number[] = []; let start = 0; for (let offset = 0; offset < artifact.bytes.byteLength; offset += 1) { if (artifact.bytes[offset] !== 0x0a) continue; const bytes = artifact.bytes.subarray(start, offset); if (bytes.byteLength === 0) throw new ApiError(500, "mesa_run_corrupt", "The event artifact contains an empty line."); let event: unknown; try { event = JSON.parse(bytes.toString("utf8")); } catch { throw new ApiError(500, "mesa_run_corrupt", "The event artifact contains invalid JSON."); } this.#verifyDomainEvent(event, run, events.length + 1); const value = event as Plain; events.push(value); const projection = Object.fromEntries(["sequence", "sim_time_days", "event_type", "phase", "turbine_id", "crew_id", "work_order_id", "correlation_id", "before_state", "after_state", "payload", "model_id", "model_revision_id", "experiment_revision_id", "preset_id", "seed"].map((key) => [key, value[key]])); semantic.update(canonicalJsonV2({ ...projection, runtime_profile: runtimeProfile })); if ((events.length - 1) % 512 === 0) offsets.push(start); start = offset + 1; } if (!events.length) throw new ApiError(500, "mesa_run_corrupt", "The event artifact is empty."); const result = { events, offsets, semanticSha256: semantic.digest("hex") }; this.#persistEventIndex({ ...declaration, byte_length: artifact.bytes.byteLength }, result); this.#eventCache.set(cacheKey, result); return result; }
  #scanFilteredEvents(projectId: string, run: any, artifact: WindArtifact, declaration: VerifiedMesaArtifact, after: number, limit: number, filters: Record<string, string | number | null>): { sourceCount: number; scanned: number; events: Plain[] } { const cacheKey = `${declaration.sha256}:${artifact.bytes.byteLength}:event-index-v1`; const memory = this.#eventCache.get(cacheKey); if (memory) { const events: Plain[] = []; let scanned = after; let examined = 0; for (let index = after; index < memory.events.length && examined < 5000 && events.length < limit; index += 1) { const event = memory.events[index]; examined += 1; scanned = Number(event.sequence); if (matchesEvent(event, filters)) events.push(event); } return { sourceCount: memory.events.length, scanned, events }; } let index = this.#readEventIndex(declaration, artifact.bytes.byteLength); if (!index) { this.#verifiedEvents(projectId, run, artifact, declaration); return this.#scanFilteredEvents(projectId, run, artifact, declaration, after, limit, filters); } const entries = index.entries as Plain[]; const target = Math.min(after + 1, Number(index.source_event_count)); let anchor = entries[0]; for (const entry of entries) { if (Number(entry.sequence) <= target) anchor = entry; else break; } let offset = Number(anchor.byte_offset); let expected = Number(anchor.sequence); let scanned = after; let examined = 0; const events: Plain[] = []; while (offset < artifact.bytes.byteLength && examined < 5000 && events.length < limit) { const newline = artifact.bytes.indexOf(0x0a, offset); if (newline < 0) throw new ApiError(500, "mesa_run_corrupt", "The indexed event artifact has no final LF."); let event: unknown; try { event = JSON.parse(artifact.bytes.subarray(offset, newline).toString("utf8")); } catch { throw new ApiError(500, "mesa_run_corrupt", "An indexed event line is invalid JSON."); } this.#verifyDomainEvent(event, run, expected); const value = event as Plain; if (expected > after) { examined += 1; scanned = expected; if (matchesEvent(value, filters)) events.push(value); } expected += 1; offset = newline + 1; } return { sourceCount: Number(index.source_event_count), scanned, events }; }
  #readEventIndex(declaration: VerifiedMesaArtifact, artifactByteLength: number): Plain | null { const path = join(this.#derivedCacheRoot("event-index"), `${declaration.sha256}.json`); if (!existsSync(path)) return null; try { const bytes = readFileSync(path); if (bytes.at(-1) !== 0x0a) throw new Error("index LF"); const value = JSON.parse(bytes.subarray(0, -1).toString("utf8")); exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "index_version", "source_sha256", "source_event_count", "artifact_byte_length", "entries", "semantic_sha256", "index_digest"], "derived_cache_unavailable"); if (!canonicalJsonV2(value).equals(bytes.subarray(0, -1)) || value.schema_id !== "riff://evidence-studio/domain-event-sparse-index/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.index_version !== "event-index-v1" || value.source_sha256 !== declaration.sha256 || value.artifact_byte_length !== artifactByteLength || !Number.isSafeInteger(value.source_event_count) || Number(value.source_event_count) < 1 || !HEX.test(String(value.semantic_sha256)) || digestWithout("eidx_", value, "index_digest") !== value.index_digest || !Array.isArray(value.entries) || value.entries.length < 1) throw new Error("index root"); let expectedSequence = 1; let priorOffset = -1; for (const entry of value.entries) { exactKeys(entry, ["sequence", "byte_offset", "sim_time_days", "event_type", "turbine_id", "crew_id", "work_order_id"], "derived_cache_unavailable"); if (entry.sequence !== expectedSequence || !Number.isSafeInteger(entry.byte_offset) || Number(entry.byte_offset) <= priorOffset || typeof entry.sim_time_days !== "number" || !Number.isFinite(entry.sim_time_days)) throw new Error("index entry"); expectedSequence += 512; priorOffset = Number(entry.byte_offset); } return value; } catch { try { unlinkSync(path); } catch {} return null; } }
  #verifyDomainEvent(value: unknown, run: any, sequence: number): asserts value is Plain { const identityFields = plain(value) && "workflow_label" in value ? REPLAY_IDENTITY_FIELDS : LEGACY_IDENTITY_FIELDS; exactKeys(value, [...identityFields, ...RAW_EVENT_FIELDS], "mesa_run_corrupt"); for (const key of identityFields) if (value[key] !== this.#replayIdentity(run)[key]) throw new ApiError(500, "mesa_run_corrupt", "A domain event identity differs from the committed run."); if (value.sequence !== sequence || value.event_id !== `event-${String(sequence).padStart(8, "0")}` || typeof value.sim_time_days !== "number" || !Number.isFinite(value.sim_time_days) || value.sim_time_days < 0 || !Number.isSafeInteger(value.phase) || !EVENT_PHASES[String(value.event_type)]?.includes(Number(value.phase)) || !plain(value.payload)) throw new ApiError(500, "mesa_run_corrupt", "A domain event violates its exact sequence/type contract."); const patterns: Record<string, RegExp> = { turbine_id: /^turbine-[0-9]{4}$/u, crew_id: /^crew-[0-9]{3}$/u, work_order_id: /^work-[0-9]{8}$/u }; for (const [key, pattern] of Object.entries(patterns)) if (value[key] !== null && (typeof value[key] !== "string" || !pattern.test(String(value[key])))) throw new ApiError(500, "mesa_run_corrupt", "A domain event contains an invalid entity ID."); if (value.correlation_id !== null && typeof value.correlation_id !== "string" || [value.before_state, value.after_state].some((item) => item !== null && !STATE_VOCABULARY.has(String(item)))) throw new ApiError(500, "mesa_run_corrupt", "A domain event contains an invalid correlation or state."); try { canonicalJsonV2(value); } catch { throw new ApiError(500, "mesa_run_corrupt", "A domain event contains an unsafe JSON value."); } }
  #persistEventIndex(declaration: VerifiedMesaArtifact, value: { events: Plain[]; offsets: number[]; semanticSha256: string }): void { const root = this.#derivedCacheRoot("event-index"); const entries = value.offsets.map((byteOffset, index) => { const event = value.events[index * 512]; return { sequence: event.sequence, byte_offset: byteOffset, sim_time_days: event.sim_time_days, event_type: event.event_type, turbine_id: event.turbine_id, crew_id: event.crew_id, work_order_id: event.work_order_id }; }); const unsigned: Plain = { schema_id: "riff://evidence-studio/domain-event-sparse-index/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, index_version: "event-index-v1", source_sha256: declaration.sha256, source_event_count: value.events.length, artifact_byte_length: declaration.byte_length, entries, semantic_sha256: value.semanticSha256, index_digest: "" }; const record = { ...unsigned, index_digest: digestWithout("eidx_", unsigned, "index_digest") }; const target = join(root, `${declaration.sha256}.json`); if (existsSync(target)) { try { const current = readFileSync(target); if (current.at(-1) === 0x0a && current.subarray(0, -1).equals(canonicalJsonV2(record))) return; } catch {} try { unlinkSync(target); } catch {} } const temporary = `${target}.${crypto.randomUUID()}.tmp`; try { writeFileSync(temporary, Buffer.concat([canonicalJsonV2(record), Buffer.from("\n")] ), { flag: "wx", mode: 0o600 }); const descriptor = openSync(temporary, "r+"); try { fsyncSync(descriptor); } finally { closeSync(descriptor); } renameSync(temporary, target); } catch { try { unlinkSync(temporary); } catch {} throw new ApiError(503, "derived_cache_unavailable", "The event index could not be written atomically."); } }
  #terminalArtifacts(terminal: NonNullable<import("./durable-project-types.ts").VerifiedMesaRunEvidence["terminal_metadata"]>): VerifiedMesaArtifact[] { return "terminal_metadata_kind" in terminal ? Object.entries(terminal.artifacts).map(([name, item]) => ({ name: name as VerifiedMesaArtifact["name"], ...item })) : terminal.artifacts; }
  #candidateRuntimeInstance(projectId: string): string { const value = this.#candidateRuntimeInstances.get(projectId); if (!value) throw new ApiError(409, "stale_candidate", "The candidate runtime instance is no longer available."); return value; }
  #buildActivationTarget(projectId: string, actorId: string, now: string, source: { model_revision_id: string; brief_revision_id: string; alignment_revision_id: string; experiment_revision_id: string }, descriptor: FramedCandidateDescriptor): any {
    const sourceBrief = this.#gate2.store.decisionBrief(projectId, source.brief_revision_id); const sourceAlignment = this.#gate2.store.alignmentMap(projectId, source.alignment_revision_id); const sourceExperiment = this.#gate2.store.experimentRevision(projectId, source.experiment_revision_id); const installed = this.#loadCandidateSourceDescriptor();
    const parameterSchema = this.#readInstalledJson(resolve(import.meta.dirname, "../../mesa_service/src/mesa_service/model_assets/wind_turbine_maintenance_framed/parameter-schema.json")); const preset = this.#readInstalledJson(resolve(import.meta.dirname, "../../mesa_service/src/mesa_service/model_assets/wind_turbine_maintenance/defaults/wind-turbine-maintenance-demo-v1.json")); this.#verifyFramedParameterContract(parameterSchema, preset);
    const properties = parameterSchema.properties as Plain; const parameterRules = Object.fromEntries(Object.entries(properties).map(([name, value]) => { const property = value as Plain; return [name, { type: property.type, ...(typeof property.minimum === "number" ? { minimum: property.minimum } : {}), ...(typeof property.maximum === "number" ? { maximum: property.maximum } : {}) }]; })); const parameterDefaults = structuredClone(preset.parameters) as Record<string, any>; const copied = structuredClone(sourceExperiment.parameters) as Record<string, any>; for (const [name, rule] of Object.entries(parameterRules) as any) { const value = copied[name]; if (rule.type === "boolean" ? typeof value !== "boolean" : typeof value !== "number" || !Number.isFinite(value) || rule.type === "integer" && !Number.isInteger(value) || value < rule.minimum || value > rule.maximum) copied[name] = parameterDefaults[name]; }
    const modelSpec = this.#readInstalledJson(resolve(import.meta.dirname, "../../mesa_service/src/mesa_service/model_assets/wind_turbine_maintenance_framed/model-spec.json")); const metric = this.#readInstalledJson(resolve(import.meta.dirname, "../../mesa_service/src/mesa_service/model_assets/wind_turbine_maintenance/metric-schema.json")); const traceability = this.#readInstalledJson(resolve(import.meta.dirname, "../../mesa_service/src/mesa_service/model_assets/wind_turbine_maintenance/traceability.json")); const allowedRefs = new Set<string>([...Object.keys(properties).map((name) => `parameter:${name}`), ...Object.keys(plain(metric.properties) ? metric.properties : {}).map((name) => `metric:${name}`), ...Object.keys(modelSpec).map((name) => `mechanism:${name}`)]); for (const family of [traceability.equipment_transitions, traceability.crew_transitions]) for (const item of Array.isArray(family) ? family : []) if (plain(item) && typeof item.target_rule === "string") allowedRefs.add(`mechanism:${item.target_rule}`);
    const executionDefaults = { horizon_days: Number(preset.horizon_days), warmup_days: Number(preset.warmup_days), seed: Number(preset.seed) }; const executionValues = sourceExperiment.execution_values; const contract: WindModelContract = { model_id: MODEL_ID, model_revision_id: descriptor.model_revision_id, preset_id: "wind-turbine-maintenance-demo-v1", parameter_defaults: parameterDefaults, execution_defaults: executionDefaults, runtime_profile: descriptor.runtime_profile, parameter_rules: parameterRules, allowed_model_refs: [...allowedRefs].sort() };
    const briefContent = { question: sourceBrief.question, decision_owner: sourceBrief.decision_owner, objective: sourceBrief.objective, constraints: sourceBrief.constraints, assumptions: sourceBrief.assumptions, non_goals: sourceBrief.non_goals, sources: sourceBrief.sources }; const briefPreimage: any = { schema_id: "riff://evidence-studio/decision-brief/activation-v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, parent_brief_revision_id: source.brief_revision_id, source_brief_revision_id: source.brief_revision_id, operation: "activation_copy", copy_rule: "exact_content_activation_copy_v1", content: briefContent, created_by_actor_id: actorId, created_at: now }; const briefId = `dbr_${sha256Hex(canonicalJsonV2(briefPreimage))}`; const briefWithId = { ...briefPreimage, decision_brief_revision_id: briefId }; const brief = { ...briefWithId, decision_brief_digest: `dbrd_${sha256Hex(canonicalJsonV2(briefWithId))}` };
    const alignmentPreimage: any = { schema_id: "riff://evidence-studio/alignment-map/framed/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, parent_alignment_revision_id: source.alignment_revision_id, brief_revision_id: briefId, model_revision_id: descriptor.model_revision_id, migration_rule: "framed_alignment_rebind_v1", mappings: sourceAlignment.entries, gaps: sourceAlignment.known_gaps, source_refs: sourceAlignment.entries.map((entry) => entry.source), created_by_actor_id: actorId, created_at: now }; const alignmentId = `amr_${sha256Hex(canonicalJsonV2(alignmentPreimage))}`; const alignmentWithId = { ...alignmentPreimage, alignment_revision_id: alignmentId }; const alignment = { ...alignmentWithId, alignment_digest: `amd_${sha256Hex(canonicalJsonV2(alignmentWithId))}` };
    const defaultsObject = { parameter_defaults: parameterDefaults, execution_defaults: executionDefaults, preset_id: "wind-turbine-maintenance-demo-v1" }; const parameterDiff = Object.keys(parameterDefaults).sort().filter((name) => canonicalJsonV2(parameterDefaults[name]).toString() !== canonicalJsonV2(copied[name]).toString()).map((name) => ({ parameter_id: name, default_value: parameterDefaults[name], current_value: copied[name] })); const executionDiff = (["horizon_days", "warmup_days", "seed"] as const).filter((field) => executionDefaults[field] !== executionValues[field]).map((field) => ({ field, default_value: executionDefaults[field], current_value: executionValues[field] })); const experimentPreimage: any = { schema_id: "riff://evidence-studio/experiment-revision/framed/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, parent_experiment_revision_id: null, operation: "create", model_id: MODEL_ID, model_revision_id: descriptor.model_revision_id, brief_revision_id: briefId, alignment_revision_id: alignmentId, preset_id: "wind-turbine-maintenance-demo-v1", defaults_digest: `dd_${sha256Hex(canonicalJsonV2(defaultsObject))}`, parameter_defaults: parameterDefaults, parameters: copied, parameter_diff: parameterDiff, execution_defaults: executionDefaults, execution_values: executionValues, execution_diff: executionDiff, runtime_profile: descriptor.runtime_profile, copy_migration_rule: "framed_parameter_copy_revalidate_v1", created_by_actor_id: actorId, created_at: now }; const experimentId = `er_${sha256Hex(canonicalJsonV2(experimentPreimage))}`; const experimentWithId = { ...experimentPreimage, experiment_revision_id: experimentId }; const experiment = { ...experimentWithId, experiment_digest: `erd_${sha256Hex(canonicalJsonV2(experimentWithId))}` };
    return { contract, tuple: { model_revision_id: descriptor.model_revision_id, brief_revision_id: briefId, alignment_revision_id: alignmentId, experiment_revision_id: experimentId }, brief: { kind: "decision_brief", id: briefId, digest: brief.decision_brief_digest, value: brief }, alignment: { kind: "alignment_map", id: alignmentId, digest: alignment.alignment_digest, value: alignment }, experiment: { kind: "experiment_revision", id: experimentId, digest: experiment.experiment_digest, value: experiment } };
  }
  #stagedRef(projectId: string, actorId: string, now: string, kind: string, id: string, value: Plain): Plain { const bytes = Buffer.concat([canonicalJsonV2(value), Buffer.from("\n")]); const recordDigest = String(value.decision_brief_digest ?? value.alignment_digest ?? value.experiment_digest); return { project_id: projectId, record_id: id, record_digest: recordDigest, canonical_bytes_sha256: sha256Hex(bytes), byte_length: bytes.byteLength, created_at: now, created_by_actor_id: actorId, record_kind: kind, record_schema_id: kind === "decision_brief" ? "riff://evidence-studio/decision-brief/activation-v1" : kind === "alignment_map" ? "riff://evidence-studio/alignment-map/framed/v1" : "riff://evidence-studio/experiment-revision/framed/v1", record_schema_version: 1 }; }
  #verifyFrozenActivationRevision(value: Plain, idField: string, digestField: string, idPrefix: string, digestPrefix: string): void { const idPreimage = { ...value }; delete idPreimage[idField]; delete idPreimage[digestField]; const expectedId = `${idPrefix}${sha256Hex(canonicalJsonV2(idPreimage))}`; const digestPreimage = { ...value }; delete digestPreimage[digestField]; const expectedDigest = `${digestPrefix}${sha256Hex(canonicalJsonV2(digestPreimage))}`; if (value[idField] !== expectedId || value[digestField] !== expectedDigest) throw new ApiError(500, "immutable_record_corrupt", "A frozen activation revision failed identity verification."); }
  #verifyCandidateCapture(projectId: string, activationId: string, descriptor: FramedCandidateDescriptor, intent: Plain, candidate: Plain, capture: Plain): void { exactKeys(capture, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "candidate_descriptor", "candidate_receipt", "candidate_bytes_digest"], "mesa_adapter_failure"); exactKeys(candidate, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "intent_digest", "expected_old_model_revision_id", "candidate_descriptor_digest", "target_model_revision_id", "bundle_protocol", "manifest_sha256", "files", "file_map_sha256", "candidate_bytes_digest", "created_at", "candidate_receipt_digest"], "mesa_adapter_failure"); if (candidate.schema_id !== "riff://mesa-wind/candidate-receipt/v1" || candidate.schema_version !== 1 || candidate.canonical_json_version !== GATE3_CANONICAL || candidate.expected_old_model_revision_id !== (intent.source as Plain).model_revision_id || candidate.bundle_protocol !== descriptor.bundle_protocol || !plain(candidate.files) || Object.keys(candidate.files).sort().join("\n") !== [...FRAMED_BUNDLE_FILES].sort().join("\n") || capture.schema_id !== "riff://mesa-wind/candidate-capture-response/v1" || capture.schema_version !== 1 || capture.canonical_json_version !== GATE3_CANONICAL || capture.activation_id !== activationId || !plain(capture.candidate_descriptor) || !plain(capture.candidate_receipt) || canonicalJsonV2(capture.candidate_descriptor).toString() !== canonicalJsonV2(descriptor).toString() || canonicalJsonV2(capture.candidate_receipt).toString() !== canonicalJsonV2(candidate).toString() || capture.candidate_bytes_digest !== candidate.candidate_bytes_digest || candidate.project_id !== projectId || candidate.activation_id !== activationId || candidate.intent_digest !== intent.intent_digest || candidate.candidate_descriptor_digest !== descriptor.descriptor_digest || candidate.target_model_revision_id !== descriptor.model_revision_id || candidate.manifest_sha256 !== descriptor.manifest_sha256 || candidate.file_map_sha256 !== descriptor.file_map_sha256 || digestWithout("acand_", candidate, "candidate_receipt_digest") !== candidate.candidate_receipt_digest) throw new ApiError(502, "mesa_adapter_failure", "The Mesa candidate capture failed integrity verification."); }
  #verifySwitchReceipt(value: Plain, request: Plain): Plain {
    exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "expected_old_model_revision_id", "target_model_revision_id", "candidate_receipt_digest", "project_event_digest", "previous_active_model_revision_id", "active_model_revision_id", "switched_at", "switch_receipt_digest"], "mesa_adapter_failure");
    if (value.schema_id !== "riff://mesa-wind/active-switch-receipt/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.activation_id !== request.activation_id || value.project_id !== request.project_id || value.expected_old_model_revision_id !== request.expected_old_model_revision_id || value.target_model_revision_id !== request.target_model_revision_id || value.candidate_receipt_digest !== request.candidate_receipt_digest || value.project_event_digest !== request.project_event_digest || value.previous_active_model_revision_id !== request.expected_old_model_revision_id || value.active_model_revision_id !== request.target_model_revision_id || typeof value.switched_at !== "string" || !Number.isFinite(Date.parse(value.switched_at)) || digestWithout("asw_", value, "switch_receipt_digest") !== value.switch_receipt_digest) throw new ApiError(502, "mesa_adapter_failure", "The Mesa switch receipt failed exact verification."); return value;
  }
  #verifyActivationStatus(value: Plain, request: Plain, switched: Plain): Plain {
    exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "status", "active_model_revision_id", "candidate_receipt_digest", "switch_receipt"], "mesa_adapter_failure");
    if (value.schema_id !== "riff://mesa-wind/activation-status/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.activation_id !== request.activation_id || value.status !== "switched" || value.active_model_revision_id !== request.target_model_revision_id || value.candidate_receipt_digest !== request.candidate_receipt_digest || !plain(value.switch_receipt)) throw new ApiError(502, "mesa_adapter_failure", "The Mesa activation status failed exact verification."); const embedded = this.#verifySwitchReceipt(value.switch_receipt, request); if (!canonicalJsonV2(embedded).equals(canonicalJsonV2(switched))) throw new ApiError(502, "mesa_adapter_failure", "The Mesa activation status differs from the switch receipt."); return value;
  }
  #verifyReconcileMarker(marker: Plain, projectId: string, activationId: string, binding: Plain, committed: import("./durable-project-types.ts").DurableProjectEvent): void {
    exactKeys(marker, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "target_binding_digest", "base_project_event_digest", "base_snapshot_revision", "switch_receipt_digest", "verified_project_target_model_revision_id", "verified_mesa_active_model_revision_id", "reconciled_at", "reconcile_digest"], "immutable_record_corrupt");
    if (marker.schema_id !== "riff://evidence-studio/activation-reconcile-marker/v1" || marker.schema_version !== 1 || marker.canonical_json_version !== GATE3_CANONICAL || marker.activation_id !== activationId || marker.project_id !== projectId || marker.target_binding_digest !== binding.target_binding_digest || marker.base_project_event_digest !== committed.event_digest || marker.base_snapshot_revision !== committed.snapshot_revision || marker.verified_project_target_model_revision_id !== (binding.target as Plain).model_revision_id || marker.verified_mesa_active_model_revision_id !== (binding.target as Plain).model_revision_id || !/^asw_[0-9a-f]{64}$/u.test(String(marker.switch_receipt_digest)) || typeof marker.reconciled_at !== "string" || !Number.isFinite(Date.parse(marker.reconciled_at)) || digestWithout("arec_", marker, "reconcile_digest") !== marker.reconcile_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation reconcile marker is corrupt.");
  }
  #verifyCandidateByteCapture(projectId: string, activationId: string, descriptor: FramedCandidateDescriptor, receiptDigest: string, candidateBytesDigest: string, capture: Plain, wire: Buffer): Map<string, Buffer> {
    if (wire.byteLength > 6 * 1024 * 1024) throw new ApiError(502, "mesa_adapter_failure", "The candidate byte-capture wire exceeds its bound."); exactKeys(capture, ["schema_id", "schema_version", "canonical_json_version", "project_id", "activation_id", "target_model_revision_id", "candidate_descriptor_digest", "candidate_receipt_digest", "manifest_sha256", "manifest", "files", "file_map_sha256", "candidate_bytes_digest", "capture_digest"], "mesa_adapter_failure");
    if (capture.schema_id !== "riff://mesa-wind/candidate-byte-capture/v1" || capture.schema_version !== 1 || capture.canonical_json_version !== GATE3_CANONICAL || capture.project_id !== projectId || capture.activation_id !== activationId || capture.target_model_revision_id !== descriptor.model_revision_id || capture.candidate_descriptor_digest !== descriptor.descriptor_digest || capture.candidate_receipt_digest !== receiptDigest || capture.manifest_sha256 !== descriptor.manifest_sha256 || capture.file_map_sha256 !== descriptor.file_map_sha256 || capture.candidate_bytes_digest !== candidateBytesDigest || digestWithout("cap_", capture, "capture_digest") !== capture.capture_digest || !plain(capture.files)) throw new ApiError(502, "mesa_adapter_failure", "The candidate byte capture identity is invalid.");
    const expected = ["model.py", "model-spec.json", "parameter-schema.json", "execution-field-schema.json", "metric-schema.json", "visualization.json", "traceability.json", "provenance.json", "defaults/source-field-service-reference.json", "defaults/wind-turbine-maintenance-demo-v1.json", "tests/microcase.json", "tests/source-transition-disposition.json"]; if (Object.keys(capture.files).sort().join("\n") !== [...expected].sort().join("\n")) throw new ApiError(502, "mesa_adapter_failure", "The candidate byte capture file set is not exact.");
    const decode = (name: string, blob: unknown): Buffer => { exactKeys(blob, ["sha256", "byte_length", "media_type", "content_encoding", "content_base64"], "mesa_adapter_failure"); if (!HEX.test(String(blob.sha256)) || !Number.isSafeInteger(blob.byte_length) || Number(blob.byte_length) < 0 || Number(blob.byte_length) > 512 * 1024 || blob.content_encoding !== "base64-rfc4648" || typeof blob.content_base64 !== "string" || !/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/u.test(blob.content_base64)) throw new ApiError(502, "mesa_adapter_failure", `The captured blob ${name} is invalid.`); const bytes = Buffer.from(blob.content_base64, "base64"); if (bytes.toString("base64") !== blob.content_base64 || bytes.byteLength !== blob.byte_length || sha256Hex(bytes) !== blob.sha256 || blob.media_type !== (name === "model.py" ? "text/x-python" : "application/json")) throw new ApiError(502, "mesa_adapter_failure", `The captured blob ${name} failed byte verification.`); return bytes; };
    const files = new Map<string, Buffer>(); const manifestBytes = decode("manifest.json", capture.manifest); files.set("manifest.json", manifestBytes); for (const name of expected) files.set(name, decode(name, capture.files[name])); const total = [...files.values()].reduce((sum, bytes) => sum + bytes.byteLength, 0); if (total > 4 * 1024 * 1024) throw new ApiError(502, "mesa_adapter_failure", "The candidate raw byte capture exceeds its bound."); if (sha256Hex(manifestBytes) !== descriptor.manifest_sha256 || manifestBytes.at(-1) !== 0x0a) throw new ApiError(502, "mesa_adapter_failure", "The captured manifest bytes differ from the descriptor.");
    let manifest: unknown; try { manifest = JSON.parse(manifestBytes.toString("utf8")); } catch { throw new ApiError(502, "mesa_adapter_failure", "The captured manifest is invalid JSON."); } exactKeys(manifest, ["schema_version", "bundle_protocol", "model_id", "model_revision_id", "runtime_profile", "files"], "mesa_adapter_failure"); if (!canonicalJsonV2(manifest).equals(manifestBytes.subarray(0, -1)) || manifest.schema_version !== 2 || manifest.bundle_protocol !== descriptor.bundle_protocol || manifest.model_id !== MODEL_ID || manifest.model_revision_id !== descriptor.model_revision_id || !plain(manifest.files) || Object.keys(manifest.files).sort().join("\n") !== [...expected].sort().join("\n")) throw new ApiError(502, "mesa_adapter_failure", "The captured framed manifest contract is invalid.");
    const reconstructedMap: Plain = {}; for (const name of expected) { const declaration = manifest.files[name]; exactKeys(declaration, ["sha256", "byte_length", "media_type"], "mesa_adapter_failure"); const bytes = files.get(name)!; if (declaration.sha256 !== sha256Hex(bytes) || declaration.byte_length !== bytes.byteLength || declaration.media_type !== (name === "model.py" ? "text/x-python" : "application/json")) throw new ApiError(502, "mesa_adapter_failure", "A captured candidate file differs from its manifest."); reconstructedMap[name] = declaration; }
    if (sha256Hex(canonicalJsonV2(reconstructedMap)) !== descriptor.file_map_sha256) throw new ApiError(502, "mesa_adapter_failure", "The captured candidate file-map digest is invalid."); const revisionPreimage = { schema_version: 2, bundle_protocol: manifest.bundle_protocol, model_id: manifest.model_id, runtime_profile: manifest.runtime_profile, files: reconstructedMap }; if (`mr_${sha256Hex(canonicalJsonV2(revisionPreimage))}` !== descriptor.model_revision_id) throw new ApiError(502, "mesa_adapter_failure", "The captured candidate revision digest is invalid.");
    const framed = createHash("sha256"); for (const name of ["manifest.json", ...[...expected].sort()]) { const path = Buffer.from(name, "utf8"); const bytes = files.get(name)!; const pathLength = Buffer.alloc(4); pathLength.writeUInt32BE(path.byteLength); const byteLength = Buffer.alloc(8); byteLength.writeBigUInt64BE(BigInt(bytes.byteLength)); framed.update(pathLength).update(path).update(byteLength).update(bytes); } if (framed.digest("hex") !== candidateBytesDigest) throw new ApiError(502, "mesa_adapter_failure", "The captured candidate framed-byte digest is invalid."); return files;
  }
  #activationReceipt(command: ProjectCommand<any>, commandDigest: string, intent: Plain, terminalStatus: "ready" | "failed_no_effect" | "failed_fenced", event: any, targetBindingDigest: string | null, reconcileDigest: string | null, safeErrorCode: string | null): Plain { const unsigned: Plain = { schema_id: "riff://evidence-studio/activation-command-receipt/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: command.project_id, command_id: command.command_id, command_digest: commandDigest, command_digest_version: "gate3-command-digest-v2", activation_id: command.command_id, intent_digest: intent.intent_digest, terminal_status: terminalStatus, event_type: terminalStatus === "ready" ? "model.activation_reconciled" : "model.activation_failed", committed_snapshot_revision: event.snapshot_revision, terminal_project_event_digest: event.event_digest, target_binding_digest: targetBindingDigest, reconcile_digest: reconcileDigest, safe_error_code: safeErrorCode, created_at: new Date().toISOString(), receipt_digest: "" }; return { ...unsigned, receipt_digest: digestWithout("acr_", unsigned, "receipt_digest") }; }
  #writeClientOutcome(projectId: string, activationId: string, commandDigest: string, httpStatus: number, httpBody: Plain, receipt: Plain): void { const unsigned: Plain = { schema_id: "riff://evidence-studio/activation-client-outcome/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, project_id: projectId, activation_id: activationId, command_id: activationId, command_digest: commandDigest, http_status: httpStatus, http_body: httpBody, command_receipt_digest: receipt.receipt_digest, created_at: new Date().toISOString(), outcome_digest: "" }; const value = { ...unsigned, outcome_digest: digestWithout("aout_", unsigned, "outcome_digest") }; this.#gate2.store.writeGate3ActivationRecord(projectId, activationId, "client-outcome.json", value); }
  #terminalEvent(projectId: string, activationId: string): import("./durable-project-types.ts").DurableProjectEvent | null {
    const reconciled = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_reconciled"); const failed = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_failed");
    if (reconciled && failed) throw new ApiError(500, "immutable_record_corrupt", "An activation has conflicting terminal events."); return failed ?? reconciled;
  }
  #completeTerminal(projectId: string, activationId: string, commandDigest: string, injectFaults = false): { status: number; body: Plain } {
    const directory = join(this.#workspaceRoot, "projects", projectId, "activations", activationId); const intent = this.#readJsonFile(join(directory, "intent.json"));
    if (intent.command_digest !== commandDigest) throw new ApiError(409, "idempotency_conflict", "The command ID is already bound to different activation content.");
    const reservationPath = join(directory, "command-reservation.json"); if (!existsSync(reservationPath)) throw new ApiError(500, "immutable_record_corrupt", "The activation command reservation is missing."); this.#replayReservation(this.#readJsonFile(reservationPath), projectId, activationId, commandDigest);
    const terminal = this.#terminalEvent(projectId, activationId); if (!terminal) throw new ApiError(500, "immutable_record_corrupt", "The activation terminal event is missing."); const ready = terminal.event_type === "model.activation_reconciled"; const terminalStatus = ready ? "ready" : String(terminal.response_projection.status) as "failed_no_effect" | "failed_fenced";
    if (!ready && !["failed_no_effect", "failed_fenced"].includes(terminalStatus)) throw new ApiError(500, "immutable_record_corrupt", "The activation failure status is invalid.");
    const bindingPath = join(directory, "target-binding.json"); const reconcilePath = join(directory, "reconcile.json"); const failurePath = join(directory, "failure.json"); const receiptPath = join(directory, "command-receipt.json"); const outcomePath = join(directory, "client-outcome.json");
    const binding = existsSync(bindingPath) ? this.#readJsonFile(bindingPath) : null; const reconcile = existsSync(reconcilePath) ? this.#readJsonFile(reconcilePath) : null; let failure = existsSync(failurePath) ? this.#readJsonFile(failurePath) : null;
    if (!ready && !failure) { const projection = terminal.response_projection as Plain; const code = typeof projection.safe_error_code === "string" ? projection.safe_error_code : "activation_recovery_failed"; const message = typeof projection.safe_error_message === "string" ? projection.safe_error_message : "Activation recovery found an unrecoverable mismatch."; const correlationId = typeof projection.correlation_id === "string" ? projection.correlation_id : crypto.randomUUID(); const failedAt = typeof projection.failed_at === "string" ? projection.failed_at : terminal.committed_at; const unsigned: Plain = { schema_id: "riff://evidence-studio/activation-failure/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, project_id: projectId, terminal_status: terminalStatus, code, message, correlation_id: correlationId, failed_at: failedAt, failure_digest: "" }; failure = { ...unsigned, failure_digest: digestWithout("afail_", unsigned, "failure_digest") }; this.#gate2.store.writeGate3ActivationRecord(projectId, activationId, "failure.json", failure); if (injectFaults) this.#fault?.("after_failure_record"); }
    const committed = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_committed"); const reconciled = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_reconciled"); this.#verifyActivationChain(projectId, activationId, intent, binding, reconcile, failure, null, committed, reconciled, terminalStatus);
    let receipt = existsSync(receiptPath) ? this.#readJsonFile(receiptPath) : null;
    if (!receipt) { const command = { project_id: projectId, command_id: activationId } as ProjectCommand<any>; receipt = this.#activationReceipt(command, commandDigest, intent, terminalStatus, terminal, binding ? String(binding.target_binding_digest) : null, ready && reconcile ? String(reconcile.reconcile_digest) : null, ready ? null : String(failure!.code)); this.#gate2.store.writeGate3ActivationRecord(projectId, activationId, "command-receipt.json", receipt); if (injectFaults) this.#fault?.(ready ? "after_ready_command_receipt" : "after_failure_command_receipt"); }
    this.#verifyActivationChain(projectId, activationId, intent, binding, reconcile, failure, receipt, committed, reconciled, terminalStatus);
    const body: Plain = ready ? { schema_id: "riff://evidence-studio/activation-command-result/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, activation_id: activationId, status: "ready", snapshot_revision: terminal.snapshot_revision, receipt_digest: receipt.receipt_digest } : { schema_id: "riff://evidence-studio/error/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, accepted: false, error: { code: terminalStatus === "failed_no_effect" ? "activation_failed_no_effect" : "activation_failed_fenced", message: terminalStatus === "failed_no_effect" ? "The activation failed without changing either model pointer." : "The activation failed and run admission remains fenced.", correlation_id: failure!.correlation_id, details: { activation_id: activationId, safe_error_code: failure!.code, snapshot_revision: terminal.snapshot_revision, receipt_digest: receipt.receipt_digest } } };
    if (!existsSync(outcomePath)) { if (injectFaults) this.#fault?.(ready ? "before_ready_client_outcome" : "before_failure_client_outcome"); this.#writeClientOutcome(projectId, activationId, commandDigest, ready ? 200 : 409, body, receipt); }
    const outcome = this.#readJsonFile(outcomePath); const result = this.#replayClientOutcome(outcome, projectId, activationId, commandDigest); let bound = outcome.command_receipt_digest === receipt.receipt_digest && result.status === (ready ? 200 : 409);
    if (ready) bound &&= canonicalJsonV2(result.body).equals(canonicalJsonV2(body));
    else { const actualError = result.body.error as Plain; const actualDetails = actualError.details as Plain; const expectedError = body.error as Plain; const expectedDetails = expectedError.details as Plain; bound &&= actualError.code === expectedError.code && actualError.correlation_id === failure!.correlation_id && [expectedError.message, failure!.message].includes(actualError.message) && canonicalJsonV2(actualDetails).equals(canonicalJsonV2(expectedDetails)); }
    if (!bound) throw new ApiError(500, "immutable_record_corrupt", "The activation client outcome differs from the authoritative terminal DAG."); return result;
  }
  #replayClientOutcome(value: Plain, projectId: string, activationId: string, commandDigest: string): { status: number; body: Plain } { exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "project_id", "activation_id", "command_id", "command_digest", "http_status", "http_body", "command_receipt_digest", "created_at", "outcome_digest"], "immutable_record_corrupt"); if (value.command_digest !== commandDigest) throw new ApiError(409, "idempotency_conflict", "The command ID is already bound to different activation content."); const success = value.http_status === 200; if (value.schema_id !== "riff://evidence-studio/activation-client-outcome/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.project_id !== projectId || value.activation_id !== activationId || value.command_id !== activationId || ![200, 409].includes(Number(value.http_status)) || !plain(value.http_body) || !/^acr_[0-9a-f]{64}$/u.test(String(value.command_receipt_digest)) || digestWithout("aout_", value, "outcome_digest") !== value.outcome_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation client outcome is corrupt."); if (success) { exactKeys(value.http_body, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "status", "snapshot_revision", "receipt_digest"], "immutable_record_corrupt"); if (value.http_body.schema_id !== "riff://evidence-studio/activation-command-result/v1" || value.http_body.activation_id !== activationId || value.http_body.status !== "ready" || value.http_body.receipt_digest !== value.command_receipt_digest) throw new ApiError(500, "immutable_record_corrupt", "The successful activation outcome body is corrupt."); } else { exactKeys(value.http_body, ["schema_id", "schema_version", "canonical_json_version", "accepted", "error"], "immutable_record_corrupt"); exactKeys(value.http_body.error, ["code", "message", "correlation_id", "details"], "immutable_record_corrupt"); if (value.http_body.schema_id !== "riff://evidence-studio/error/v1" || value.http_body.accepted !== false || !["activation_failed_no_effect", "activation_failed_fenced"].includes(String((value.http_body.error as Plain).code)) || !plain((value.http_body.error as Plain).details) || ((value.http_body.error as Plain).details as Plain).receipt_digest !== value.command_receipt_digest) throw new ApiError(500, "immutable_record_corrupt", "The failed activation outcome body is corrupt."); } return { status: Number(value.http_status), body: structuredClone(value.http_body) }; }
  #replayReservation(value: Plain, projectId: string, activationId: string, commandDigest: string): { status: number; body: Plain } { exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "project_id", "activation_id", "command_id", "command_digest", "http_status", "http_body", "created_at", "reservation_digest"], "immutable_record_corrupt"); if (value.command_digest !== commandDigest) throw new ApiError(409, "idempotency_conflict", "The command ID is already bound to different activation content."); if (value.schema_id !== "riff://evidence-studio/activation-command-reservation/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.project_id !== projectId || value.activation_id !== activationId || value.command_id !== activationId || value.http_status !== 202 || !plain(value.http_body) || digestWithout("ares_", value, "reservation_digest") !== value.reservation_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation command reservation is corrupt."); exactKeys(value.http_body, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "status", "snapshot_revision"], "immutable_record_corrupt"); if (value.http_body.schema_id !== "riff://evidence-studio/activation-command-pending/v1" || value.http_body.schema_version !== 1 || value.http_body.canonical_json_version !== GATE3_CANONICAL || value.http_body.activation_id !== activationId || value.http_body.status !== "authorizing" || !Number.isSafeInteger(value.http_body.snapshot_revision)) throw new ApiError(500, "immutable_record_corrupt", "The activation pending body is corrupt."); return { status: 202, body: structuredClone(value.http_body) }; }
  #authoritativeActivation(projectId: string): { activationId: string; status: "authorizing" | "mesa_switch_pending" | "ready" | "failed_fenced"; event: import("./durable-project-types.ts").DurableProjectEvent } | null { const events = this.#gate2.store.activationEvents(projectId); const ready: string[] = []; const latest = new Map<string, { status: "authorizing" | "mesa_switch_pending" | "ready" | "failed_fenced"; event: import("./durable-project-types.ts").DurableProjectEvent }>(); let current: string | null = null; for (const event of events) { const id = event.command_id; if (event.event_type === "model.activation_authorized") { latest.set(id, { status: "authorizing", event }); current = id; } else if (event.event_type === "model.activation_committed") { latest.set(id, { status: "mesa_switch_pending", event }); current = id; } else if (event.event_type === "model.activation_reconciled") { latest.set(id, { status: "ready", event }); if (!ready.includes(id)) ready.push(id); current = id; } else if (event.event_type === "model.activation_failed") { if (event.response_projection.status === "failed_no_effect") { latest.delete(id); current = [...ready].reverse().find((item) => item !== id) ?? null; } else { latest.set(id, { status: "failed_fenced", event }); current = id; } } } if (!current) return null; const value = latest.get(current); if (!value) return null; return { activationId: current, status: value.status, event: value.event }; }
  #verifyActivationReceipt(receipt: Plain, projectId: string, activationId: string, intent: Plain, terminalEvent: import("./durable-project-types.ts").DurableProjectEvent): void { exactKeys(receipt, ["schema_id", "schema_version", "canonical_json_version", "project_id", "command_id", "command_digest", "command_digest_version", "activation_id", "intent_digest", "terminal_status", "event_type", "committed_snapshot_revision", "terminal_project_event_digest", "target_binding_digest", "reconcile_digest", "safe_error_code", "created_at", "receipt_digest"], "immutable_record_corrupt"); const ready = receipt.terminal_status === "ready"; if (receipt.schema_id !== "riff://evidence-studio/activation-command-receipt/v1" || receipt.schema_version !== 1 || receipt.canonical_json_version !== GATE3_CANONICAL || receipt.project_id !== projectId || receipt.command_id !== activationId || receipt.activation_id !== activationId || receipt.command_digest !== intent.command_digest || receipt.command_digest_version !== "gate3-command-digest-v2" || receipt.intent_digest !== intent.intent_digest || !["ready", "failed_no_effect", "failed_fenced"].includes(String(receipt.terminal_status)) || receipt.event_type !== (ready ? "model.activation_reconciled" : "model.activation_failed") || terminalEvent.event_type !== receipt.event_type || receipt.committed_snapshot_revision !== terminalEvent.snapshot_revision || receipt.terminal_project_event_digest !== terminalEvent.event_digest || (ready ? receipt.safe_error_code !== null || !/^arec_[0-9a-f]{64}$/u.test(String(receipt.reconcile_digest)) : typeof receipt.safe_error_code !== "string" || receipt.reconcile_digest !== null) || digestWithout("acr_", receipt, "receipt_digest") !== receipt.receipt_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation command receipt is corrupt."); }
  #verifyActivationChain(projectId: string, activationId: string, intent: Plain, binding: Plain | null, reconcile: Plain | null, failure: Plain | null, receipt: Plain | null, committed: import("./durable-project-types.ts").DurableProjectEvent | null, reconciled: import("./durable-project-types.ts").DurableProjectEvent | null, status: string): void {
    exactKeys(intent, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "command_id", "command_digest", "command_digest_version", "base_snapshot_revision", "expected_event_head_digest", "authorized_actor_id", "source", "planned_target", "staging_manifest_digest", "staged_record_refs", "expected_candidate_descriptor_digest", "runtime_handshake_digest", "runtime_instance_id", "candidate_source_revision", "expected_active_model_revision_id", "created_at", "intent_digest"], "immutable_record_corrupt"); const authorized = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_authorized");
    if (!authorized || intent.schema_id !== "riff://evidence-studio/activation-intent/v1" || intent.schema_version !== 1 || intent.canonical_json_version !== GATE3_CANONICAL || intent.activation_id !== activationId || intent.project_id !== projectId || intent.command_id !== activationId || intent.command_digest !== authorized.command_digest || intent.command_digest_version !== "gate3-command-digest-v2" || intent.base_snapshot_revision !== authorized.previous_snapshot_revision || intent.expected_event_head_digest !== authorized.previous_event_digest || digestWithout("aint_", intent, "intent_digest") !== intent.intent_digest || !plain(intent.source) || !plain(intent.planned_target) || !plain(intent.staged_record_refs)) throw new ApiError(500, "immutable_record_corrupt", "The activation intent/event binding is corrupt.");
    const expectedAuthRefs = [{ kind: "activation_staging_manifest", id: activationId, digest: intent.staging_manifest_digest }, { kind: "activation_intent", id: activationId, digest: intent.intent_digest }]; if (!canonicalJsonV2(authorized.record_refs).equals(canonicalJsonV2(expectedAuthRefs))) throw new ApiError(500, "immutable_record_corrupt", "The activation authorization refs are not exact or ordered.");
    const stagingRoot = join(this.#workspaceRoot, "projects", projectId, "activation-staging", activationId); const manifest = this.#readJsonFile(join(stagingRoot, "staging-manifest.json")); exactKeys(manifest, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "created_at", "created_by_actor_id", "source", "target", "brief", "alignment", "experiment", "staging_manifest_digest"], "immutable_record_corrupt"); if (manifest.staging_manifest_digest !== intent.staging_manifest_digest || manifest.created_at !== intent.created_at || manifest.created_by_actor_id !== intent.authorized_actor_id || !canonicalJsonV2(manifest.source).equals(canonicalJsonV2(intent.source)) || !canonicalJsonV2(manifest.target).equals(canonicalJsonV2(intent.planned_target)) || digestWithout("astage_", manifest, "staging_manifest_digest") !== manifest.staging_manifest_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation staging manifest is corrupt.");
    const stagedKinds = [["brief", "brief.json", "decision_brief_revision_id", "decision_brief_digest", "dbr_", "dbrd_"], ["alignment", "alignment.json", "alignment_revision_id", "alignment_digest", "amr_", "amd_"], ["experiment", "experiment.json", "experiment_revision_id", "experiment_digest", "er_", "erd_"]] as const; for (const [kind, file, idField, digestField, idPrefix, digestPrefix] of stagedKinds) { const record = this.#readJsonFile(join(stagingRoot, file)); this.#verifyFrozenActivationRevision(record, idField, digestField, idPrefix, digestPrefix); const ref = (intent.staged_record_refs as Plain)[kind]; if (!plain(ref) || ref.record_id !== record[idField] || ref.record_digest !== record[digestField] || ref.canonical_bytes_sha256 !== sha256Hex(Buffer.concat([canonicalJsonV2(record), Buffer.from("\n")])) || ref.byte_length !== canonicalJsonV2(record).byteLength + 1 || ref.project_id !== projectId || ref.created_at !== intent.created_at || ref.created_by_actor_id !== intent.authorized_actor_id || !canonicalJsonV2((manifest as Plain)[kind]).toString().includes(String(record[idField]))) throw new ApiError(500, "immutable_record_corrupt", "A staged activation record/ref is corrupt."); }
    if (binding) {
      exactKeys(binding, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "source", "target", "base_snapshot_revision", "base_project_event_digest", "intent_digest", "staging_manifest_digest", "staged_record_refs", "candidate_receipt_digest", "captured_candidate_bytes_digest", "target_binding_digest"], "immutable_record_corrupt");
      if (!committed || binding.schema_id !== "riff://evidence-studio/activation-target-binding/v1" || binding.schema_version !== 1 || binding.canonical_json_version !== GATE3_CANONICAL || binding.activation_id !== activationId || binding.project_id !== projectId || binding.intent_digest !== intent.intent_digest || binding.staging_manifest_digest !== intent.staging_manifest_digest || binding.base_snapshot_revision !== committed.previous_snapshot_revision || binding.base_project_event_digest !== committed.previous_event_digest || !plain(binding.source) || !plain(binding.target) || !canonicalJsonV2(binding.source).equals(canonicalJsonV2(intent.source)) || !canonicalJsonV2(binding.target).equals(canonicalJsonV2(intent.planned_target)) || !canonicalJsonV2(binding.staged_record_refs).equals(canonicalJsonV2(intent.staged_record_refs)) || !/^acand_[0-9a-f]{64}$/u.test(String(binding.candidate_receipt_digest)) || !HEX.test(String(binding.captured_candidate_bytes_digest)) || digestWithout("atb_", binding, "target_binding_digest") !== binding.target_binding_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation target binding is corrupt.");
      exactKeys(binding.source, ["model_revision_id", "brief_revision_id", "alignment_revision_id", "experiment_revision_id"], "immutable_record_corrupt"); exactKeys(binding.target, ["model_revision_id", "brief_revision_id", "alignment_revision_id", "experiment_revision_id"], "immutable_record_corrupt");
      const refs = intent.staged_record_refs as Plain; const expectedCommitRefs = [{ kind: "activation_target_binding", id: activationId, digest: binding.target_binding_digest }, { kind: "decision_brief_revision", id: (refs.brief as Plain).record_id, digest: (refs.brief as Plain).record_digest }, { kind: "alignment_map_revision", id: (refs.alignment as Plain).record_id, digest: (refs.alignment as Plain).record_digest }, { kind: "experiment_revision", id: (refs.experiment as Plain).record_id, digest: (refs.experiment as Plain).record_digest }];
      const committedState = committed.state_patch[0]?.value; const expectedCurrent = { decision_brief_revision_id: binding.target.brief_revision_id, alignment_map_revision_id: binding.target.alignment_revision_id, model_revision_id: binding.target.model_revision_id, experiment_revision_id: binding.target.experiment_revision_id, run_id: null };
      if (!canonicalJsonV2(committed.record_refs).equals(canonicalJsonV2(expectedCommitRefs)) || committed.command_digest !== intent.command_digest || committed.initiator !== "system" || committed.system_component !== "backend_model_reconciler" || committed.response_status !== 200 || !canonicalJsonV2(committed.response_projection).equals(canonicalJsonV2({ activation_id: activationId, status: "mesa_switch_pending" })) || !plain(committedState) || committedState.phase !== "review" || !canonicalJsonV2(committedState.current).equals(canonicalJsonV2(expectedCurrent))) throw new ApiError(500, "immutable_record_corrupt", "The activation commit event does not bind the exact target state and ordered refs.");
    }
    else if (committed) throw new ApiError(500, "immutable_record_corrupt", "The committed activation target binding is missing.");
    if (reconcile) { if (!binding || !committed || !reconciled || !plain(binding.target)) throw new ApiError(500, "immutable_record_corrupt", "The reconcile chain is incomplete."); this.#verifyReconcileMarker(reconcile, projectId, activationId, binding, committed); const reconciledState = reconciled.state_patch[0]?.value; const expectedCurrent = { decision_brief_revision_id: binding.target.brief_revision_id, alignment_map_revision_id: binding.target.alignment_revision_id, model_revision_id: binding.target.model_revision_id, experiment_revision_id: binding.target.experiment_revision_id, run_id: null }; if (!canonicalJsonV2(reconciled.record_refs).equals(canonicalJsonV2([{ kind: "activation_reconcile_marker", id: activationId, digest: reconcile.reconcile_digest }])) || reconciled.command_digest !== intent.command_digest || reconciled.previous_snapshot_revision !== committed.snapshot_revision || reconciled.initiator !== "system" || reconciled.system_component !== "backend_model_reconciler" || reconciled.response_status !== 200 || !canonicalJsonV2(reconciled.response_projection).equals(canonicalJsonV2({ activation_id: activationId, status: "ready" })) || !plain(reconciledState) || reconciledState.phase !== "review" || !canonicalJsonV2(reconciledState.current).equals(canonicalJsonV2(expectedCurrent))) throw new ApiError(500, "immutable_record_corrupt", "The activation reconcile event is corrupt."); }
    else if (reconciled || status === "ready") throw new ApiError(500, "immutable_record_corrupt", "The ready activation reconcile marker is missing.");
    if (failure) { exactKeys(failure, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "terminal_status", "code", "message", "correlation_id", "failed_at", "failure_digest"], "immutable_record_corrupt"); const failedEvent = this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_failed"); const response = failedEvent?.response_projection as Plain | undefined; if (failure.schema_id !== "riff://evidence-studio/activation-failure/v1" || failure.schema_version !== 1 || failure.canonical_json_version !== GATE3_CANONICAL || failure.activation_id !== activationId || failure.project_id !== projectId || !failedEvent || failure.terminal_status !== failedEvent.response_projection.status || failure.code !== failedEvent.response_projection.safe_error_code || response?.safe_error_message !== undefined && failure.message !== response.safe_error_message || response?.correlation_id !== undefined && failure.correlation_id !== response.correlation_id || response?.failed_at !== undefined && failure.failed_at !== response.failed_at || typeof failure.message !== "string" || typeof failure.correlation_id !== "string" || typeof failure.failed_at !== "string" || !["failed_no_effect", "failed_fenced"].includes(String(failure.terminal_status)) || digestWithout("afail_", failure, "failure_digest") !== failure.failure_digest) throw new ApiError(500, "immutable_record_corrupt", "The activation failure record is corrupt."); }
    if (receipt) { const terminalEvent = receipt.event_type === "model.activation_reconciled" ? reconciled : this.#gate2.store.projectEventByCommand(projectId, activationId, "model.activation_failed"); if (!terminalEvent) throw new ApiError(500, "immutable_record_corrupt", "The activation receipt terminal event is missing."); this.#verifyActivationReceipt(receipt, projectId, activationId, intent, terminalEvent); const expectedBinding = binding?.target_binding_digest ?? null; const expectedReconcile = receipt.terminal_status === "ready" ? reconcile?.reconcile_digest ?? null : null; if (receipt.target_binding_digest !== expectedBinding || receipt.reconcile_digest !== expectedReconcile || failure && receipt.terminal_status !== failure.terminal_status || !failure && receipt.terminal_status !== "ready") throw new ApiError(500, "immutable_record_corrupt", "The activation receipt DAG edges differ from terminal records."); }
  }
  #assertRunAdmission(projectId: string, modelRevisionId: string): void { const activation = this.#activationProjection(projectId); if (!activation) return; const value = activation.projection; if (value.status !== "ready" || value.run_admission_fenced || value.target.model_revision_id !== modelRevisionId) throw new ApiError(409, "model_activation_fenced", "Run admission is fenced while model activation is unresolved or pointers differ."); }
  #verifiedActiveExperimentContract(projectId: string, modelRevisionId: string): { contract: WindModelContract; root_experiment_revision_id: string } {
    const activation = this.#activationProjection(projectId); const value = activation?.projection;
    if (!value || value.status !== "ready" || value.run_admission_fenced || !plain(value.target) || value.target.model_revision_id !== modelRevisionId) throw new ApiError(409, "model_activation_fenced", "Framed experiment edits require an exact ready activation target.");
    exactKeys(value.target, ["model_revision_id", "brief_revision_id", "alignment_revision_id", "experiment_revision_id"], "immutable_record_corrupt");
    if (!/^er_[0-9a-f]{64}$/u.test(String(value.target.experiment_revision_id))) throw new ApiError(500, "immutable_record_corrupt", "The ready activation target experiment identity is invalid.");
    this.#verifyPersistedSwitchAuthority(projectId, value);
    const snapshot = this.#gate2.store.snapshot(projectId); if (snapshot.current.model_revision_id !== modelRevisionId) throw new ApiError(409, "upstream_revision_mismatch", "The framed experiment model is not current."); this.modelViewSources(projectId, modelRevisionId); const verified = verifyFramedWindBundle(this.#workspaceRoot, projectId); if (verified.contract.model_revision_id !== modelRevisionId) throw new ApiError(409, "active_model_revision_drift", "The active model pointer differs from the framed experiment model."); return { contract: verified.contract, root_experiment_revision_id: String(value.target.experiment_revision_id) };
  }
  #verifyPersistedSwitchAuthority(projectId: string, activation: Plain): void {
    const activationId = String(activation.activation_id); const root = join(this.#workspaceRoot, "projects", projectId, "wind", "switch-receipts"); const request = this.#readJsonFile(join(root, `${activationId}.request.json`));
    exactKeys(request, ["schema_id", "schema_version", "canonical_json_version", "activation_id", "project_id", "expected_old_model_revision_id", "target_model_revision_id", "candidate_receipt_digest", "project_event_digest"], "immutable_record_corrupt");
    if (request.schema_id !== "riff://mesa-wind/active-cas-request/v1" || request.schema_version !== 1 || request.canonical_json_version !== GATE3_CANONICAL || request.activation_id !== activationId || request.project_id !== projectId || request.expected_old_model_revision_id !== (activation.source as Plain).model_revision_id || request.target_model_revision_id !== (activation.target as Plain).model_revision_id || request.candidate_receipt_digest !== activation.candidate_digest || request.project_event_digest !== activation.project_event_digest) throw new ApiError(500, "immutable_record_corrupt", "The persisted Mesa switch request differs from the ready activation DAG.");
    const switched = this.#readJsonFile(join(root, `${activationId}.json`)); let verified: Plain; try { verified = this.#verifySwitchReceipt(switched, request); } catch { throw new ApiError(500, "immutable_record_corrupt", "The persisted Mesa switch receipt differs from the ready activation DAG."); }
    if (verified.switch_receipt_digest !== activation.switch_receipt_digest) throw new ApiError(500, "immutable_record_corrupt", "The reconcile marker does not bind the persisted Mesa switch receipt.");
  }
  #activationProjection(projectId: string): { projection: any; receipt: Plain | null } | null { const authority = this.#authoritativeActivation(projectId); if (!authority) return null; const directory = join(this.#workspaceRoot, "projects", projectId, "activations", authority.activationId); const intent = this.#readJsonFile(join(directory, "intent.json")); const bindingPath = join(directory, "target-binding.json"); const reconcilePath = join(directory, "reconcile.json"); const failurePath = join(directory, "failure.json"); const receiptPath = join(directory, "command-receipt.json"); const outcomePath = join(directory, "client-outcome.json"); const reservationPath = join(directory, "command-reservation.json"); const binding = existsSync(bindingPath) ? this.#readJsonFile(bindingPath) : null; const reconcile = existsSync(reconcilePath) ? this.#readJsonFile(reconcilePath) : null; const failure = existsSync(failurePath) ? this.#readJsonFile(failurePath) : null; const receipt = existsSync(receiptPath) ? this.#readJsonFile(receiptPath) : null; const commit = this.#gate2.store.projectEventByCommand(projectId, authority.activationId, "model.activation_committed"); const reconciled = this.#gate2.store.projectEventByCommand(projectId, authority.activationId, "model.activation_reconciled"); this.#verifyActivationChain(projectId, authority.activationId, intent, binding, reconcile, failure, receipt, commit, reconciled, authority.status); if (!existsSync(reservationPath)) throw new ApiError(500, "immutable_record_corrupt", "The activation command reservation is missing."); this.#replayReservation(this.#readJsonFile(reservationPath), projectId, authority.activationId, String(intent.command_digest)); if (receipt) { if (!existsSync(outcomePath)) throw new ApiError(500, "immutable_record_corrupt", "The activation client outcome is missing."); const outcome = this.#readJsonFile(outcomePath); this.#replayClientOutcome(outcome, projectId, authority.activationId, String(intent.command_digest)); if (outcome.command_receipt_digest !== receipt.receipt_digest) throw new ApiError(500, "immutable_record_corrupt", "The client outcome does not bind the activation receipt."); } const status = authority.status; return { receipt, projection: { activation_id: authority.activationId, source: intent.source, target: intent.planned_target, status, run_admission_fenced: status !== "ready", safe_error: status === "failed_fenced" && failure ? { code: failure.code, message: failure.message, correlation_id: failure.correlation_id } : null, intent_digest: intent.intent_digest, candidate_digest: binding?.candidate_receipt_digest ?? null, project_event_digest: commit?.event_digest ?? null, switch_receipt_digest: reconcile?.switch_receipt_digest ?? null, reconcile_digest: reconcile?.reconcile_digest ?? null } }; }
  #artifactSource(projectId: string, runId: string, run: any, artifact: VerifiedMesaArtifact): SourceDescriptor { return { schema_id: "riff://evidence-studio/source-descriptor/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, source_kind: "run_artifact", logical_name: artifact.name, sha256: artifact.sha256, identity: { project_id: projectId, model_revision_id: run.model_revision_id, brief_revision_id: run.brief_revision_id, alignment_revision_id: run.alignment_revision_id, experiment_revision_id: run.experiment_revision_id, run_id: runId }, href: `/api/projects/${encodeURIComponent(projectId)}/artifacts/${encodeURIComponent(artifact.artifact_id)}` }; }
  #runIdentity(run: any): Plain { return { run_id: run.run_id, model_id: MODEL_ID, model_revision_id: run.model_revision_id, brief_revision_id: run.brief_revision_id, alignment_revision_id: run.alignment_revision_id, experiment_revision_id: run.experiment_revision_id, preset_id: run.preset_id, seed: run.seed, policy_snapshot_digest: run.policy_snapshot_digest, run_admission_digest: run.run_admission_digest, run_intent_digest: run.run_intent_digest }; }
  #replayIdentity(run: any): Plain { return { project_id: run.project_id, run_id: run.run_id, model_id: run.model_id, model_revision_id: run.model_revision_id, brief_revision_id: run.brief_revision_id, alignment_revision_id: run.alignment_revision_id, experiment_revision_id: run.experiment_revision_id, preset_id: run.preset_id, seed: run.seed, visibility: run.visibility, trust_label: run.trust_label, workflow_label: run.workflow_label, policy_snapshot_digest: run.policy_snapshot_digest, run_admission_digest: run.run_admission_digest }; }
  #assertArtifactIdentity(value: Plain, run: any, nested: boolean): void { const identity = this.#replayIdentity(run); const embedded = nested ? value.identity : value; if (!plain(embedded)) throw new ApiError(500, "mesa_run_corrupt", "A run artifact omits its exact identity."); if (nested && Object.keys(embedded).sort().join("\n") !== [...REPLAY_IDENTITY_FIELDS].sort().join("\n")) throw new ApiError(500, "mesa_run_corrupt", "A run artifact identity keyset is not exact."); for (const [key, expected] of Object.entries(identity)) if (embedded[key] !== expected) throw new ApiError(500, "mesa_run_corrupt", "A run artifact identity differs from terminal evidence."); }
  #assertRowRunIdentity(value: Plain, run: any, exact = false): void { const identity = this.#replayIdentity(run); for (const [key, expected] of Object.entries(identity)) if (value[key] !== expected) throw new ApiError(500, "mesa_run_corrupt", "A run row omits or differs from terminal identity."); if (exact) for (const key of REPLAY_IDENTITY_FIELDS) if (!(key in value)) throw new ApiError(500, "mesa_run_corrupt", "A run row identity is incomplete."); }
  #labels(value: unknown, expected: readonly string[], name: string): string[] { if (!Array.isArray(value) || !canonicalJsonV2(value).equals(canonicalJsonV2(expected))) throw new ApiError(500, "mesa_run_corrupt", `The ${name} evidence is missing, reordered, or substituted.`); return [...value] as string[]; }
  #metricProperties(projectId: string, run: any): Plain { let schema: unknown; try { schema = this.modelViewSources(projectId, run.model_revision_id).metric_schema; } catch (error) { if (!(error instanceof ApiError) || error.code !== "view_contract_unavailable") throw error; const bytes = this.#readBundleFile(this.#bundleRoot(projectId, run.model_revision_id), "metric-schema.json"); schema = this.#readJsonBytes(bytes); } if (!plain(schema) || !plain(schema.properties)) throw new ApiError(500, "mesa_run_corrupt", "The metric schema is unavailable or invalid."); return schema.properties; }
  #verifyMetricProjection(value: unknown, properties: Plain): asserts value is Plain { if (!plain(value) || Object.keys(value).sort().join("\n") !== Object.keys(properties).sort().join("\n")) throw new ApiError(500, "mesa_run_corrupt", "A metric projection does not match the exact metric schema."); for (const [name, declaration] of Object.entries(properties)) { if (!plain(declaration)) throw new ApiError(500, "mesa_run_corrupt", "A metric declaration is invalid."); const item = value[name]; if (declaration.type === "boolean" ? typeof item !== "boolean" : typeof item !== "number" || !Number.isFinite(item) || declaration.type === "integer" && !Number.isInteger(item)) throw new ApiError(500, "mesa_run_corrupt", "A metric value has the wrong exact type."); } }
  #verifySummary(summary: Plain, run: any, properties: Plain): void { const keys = [...REPLAY_IDENTITY_FIELDS, "claim_labels", "measurement_window_days", "seed_count", "minimum_availability_fraction", "minimum_availability_met", "staffing_recommendation", "metrics", "annualized_maintenance_cost", "annualized_operating_revenue", "annualized_maintenance_expense", "annualized_profit", "non_claims"]; exactKeys(summary, keys, "mesa_run_corrupt"); this.#assertArtifactIdentity(summary, run, false); this.#labels(summary.claim_labels, CLAIM_LABELS, "claim labels"); this.#labels(summary.non_claims, NON_CLAIMS, "non-claims"); this.#verifyMetricProjection(summary.metrics, properties); if (!Number.isSafeInteger(summary.measurement_window_days) || Number(summary.measurement_window_days) < 1 || summary.seed_count !== 1 || typeof summary.minimum_availability_fraction !== "number" || !Number.isFinite(summary.minimum_availability_fraction) || typeof summary.minimum_availability_met !== "boolean" || summary.staffing_recommendation !== null || [summary.annualized_maintenance_cost, summary.annualized_operating_revenue, summary.annualized_maintenance_expense, summary.annualized_profit].some((item) => typeof item !== "number" || !Number.isFinite(item))) throw new ApiError(500, "mesa_run_corrupt", "The framed summary contract is invalid."); }
  #verifyReplayEventBinding(projectId: string, run: any, manifest: Plain, verified: { kind: string; frames: Plain[]; ranges: Plain[] }, artifact: WindArtifact, indexed: { events: Plain[]; semanticSha256: string }): void { if (verified.kind === "legacy_frameless") { if (manifest.event_count !== indexed.events.length || manifest.canonical_event_sha256 !== indexed.semanticSha256) throw new ApiError(500, "mesa_run_corrupt", "The legacy replay semantic event binding is invalid."); return; } const source = manifest.event_source as Plain; if (source.byte_length !== artifact.bytes.byteLength || source.event_count !== indexed.events.length || source.raw_sha256 !== sha256Hex(artifact.bytes) || source.semantic_sha256 !== indexed.semanticSha256) throw new ApiError(500, "mesa_run_corrupt", "The framed replay event-source binding is invalid."); const experiment = this.#gate2.store.experimentRevision(projectId, run.experiment_revision_id) as any; const execution = experiment.execution_values; const parameters = experiment.parameters; if (!plain(execution) || !plain(parameters) || !Number.isSafeInteger(execution.horizon_days) || !Number.isSafeInteger(execution.warmup_days)) throw new ApiError(500, "mesa_run_corrupt", "The framed experiment source is invalid."); const horizon = Number(execution.horizon_days); const warmup = Number(execution.warmup_days); const count = Math.min(120, horizon + 1); const days = Array.from({ length: count }, (_, index) => Math.floor(index * horizon / (count - 1))); if (warmup > 0 && warmup < horizon && !days.includes(warmup)) { let selected = 1; for (let index = 2; index < days.length - 1; index += 1) if (Math.abs(days[index] - warmup) < Math.abs(days[selected] - warmup) || Math.abs(days[index] - warmup) === Math.abs(days[selected] - warmup) && days[index] < days[selected]) selected = index; days[selected] = warmup; days.sort((a, b) => a - b); } const expectedDays = [...new Set(days)]; if (verified.kind === "complete" && !canonicalJsonV2(manifest.sample_days).equals(canonicalJsonV2(expectedDays))) throw new ApiError(500, "mesa_run_corrupt", "The replay sampling algorithm output is invalid."); if (verified.kind !== "complete") return; const runtimeProfile = this.#gate2.store.modelContract(projectId, run.model_revision_id).runtime_profile; const projections = indexed.events.map((event) => ({ ...Object.fromEntries(["sequence", "sim_time_days", "event_type", "phase", "turbine_id", "crew_id", "work_order_id", "correlation_id", "before_state", "after_state", "payload", "model_id", "model_revision_id", "experiment_revision_id", "preset_id", "seed"].map((key) => [key, event[key]])), runtime_profile: runtimeProfile })); for (let index = 0; index < verified.frames.length; index += 1) { const frame = verified.frames[index]; const range = verified.ranges[index]; const first = Number(range.first_sequence); const last = Number(range.last_sequence); const segment = artifact.bytes.subarray(Number(range.byte_offset), Number(range.byte_offset) + Number(range.byte_length)); const rangeSemantic = createHash("sha256"); for (let sequence = first; sequence <= last; sequence += 1) rangeSemantic.update(canonicalJsonV2(projections[sequence - 1])); const terminalEvent = indexed.events[last - 1]; if (segment.at(-1) !== 0x0a || sha256Hex(segment) !== range.raw_range_sha256 || rangeSemantic.digest("hex") !== range.semantic_range_sha256 || terminalEvent.event_type !== "daily_snapshot" || terminalEvent.phase !== 50 || !plain(terminalEvent.payload) || !canonicalJsonV2(terminalEvent.payload.snapshot).equals(canonicalJsonV2(frame.daily_metrics)) || terminalEvent.payload.frame_state_sha256 !== frame.frame_state_sha256) throw new ApiError(500, "mesa_run_corrupt", "A replay frame does not bind its exact source event range."); const expectedPhase = frame.day === horizon ? "horizon_end" : Number(frame.day) < warmup ? "warmup" : "measurement"; if (frame.phase !== expectedPhase || (frame.queues as Plain).corrective !== (frame.daily_metrics as Plain).corrective_queue_length || (frame.queues as Plain).planned !== (frame.daily_metrics as Plain).planned_queue_length) throw new ApiError(500, "mesa_run_corrupt", "A replay frame phase or queue aggregate is invalid."); const turbines = frame.turbines as Plain[]; const crews = frame.crews as Plain[]; const turbineStates = new Set(["operating", "failed_waiting", "corrective_repair", "planned_maintenance", "major_replacement"]); const crewStates = new Set(["idle", "driving_to_work", "working", "driving_home"]); for (const item of turbines) if (!turbineStates.has(String(item.state)) || typeof item.x_km !== "number" || typeof item.y_km !== "number" || item.x_km < 0 || item.y_km < 0 || item.x_km > Number(parameters.farm_width_km) || item.y_km > Number(parameters.farm_height_km)) throw new ApiError(500, "mesa_run_corrupt", "A replay turbine state or coordinate is invalid."); for (const item of crews) if (!crewStates.has(String(item.state)) || typeof item.x_km !== "number" || typeof item.y_km !== "number" || item.x_km < 0 || item.y_km < 0 || item.x_km > Number(parameters.farm_width_km) || item.y_km > Number(parameters.farm_height_km)) throw new ApiError(500, "mesa_run_corrupt", "A replay crew state or coordinate is invalid."); const stateMetrics: Record<string, string> = { operating: "operating_count", failed_waiting: "failed_waiting_count", corrective_repair: "corrective_repair_count", planned_maintenance: "planned_maintenance_count", major_replacement: "major_replacement_count" }; const crewMetrics: Record<string, string> = { idle: "idle_crew_count", driving_to_work: "driving_to_work_crew_count", working: "working_crew_count", driving_home: "driving_home_crew_count" }; for (const [state, metric] of Object.entries(stateMetrics)) if (turbines.filter((item) => item.state === state).length !== (frame.daily_metrics as Plain)[metric]) throw new ApiError(500, "mesa_run_corrupt", "Replay turbine aggregates differ from daily metrics."); for (const [state, metric] of Object.entries(crewMetrics)) if (crews.filter((item) => item.state === state).length !== (frame.daily_metrics as Plain)[metric]) throw new ApiError(500, "mesa_run_corrupt", "Replay crew aggregates differ from daily metrics."); const preimage = { schema_id: "riff://wind-turbine-maintenance/replay-frame-state/v1", schema_version: 1, canonical_json_version: GATE3_CANONICAL, model_id: run.model_id, model_revision_id: run.model_revision_id, experiment_revision_id: run.experiment_revision_id, preset_id: run.preset_id, seed: run.seed, day: frame.day, phase: frame.phase, depot: frame.depot, turbines: frame.turbines, crews: frame.crews, queues: frame.queues, daily_metrics: frame.daily_metrics }; if (`fs_${sha256Hex(canonicalJsonV2(preimage))}` !== frame.frame_state_sha256) throw new ApiError(500, "mesa_run_corrupt", "A replay frame-state digest is invalid."); } }
  #verifyReplayManifest(manifest: Plain, run: any, eventDeclaration: VerifiedMesaArtifact, properties: Plain): { kind: "complete" | "unavailable_population_limit" | "legacy_frameless"; frames: Plain[]; ranges: Plain[]; claimLabels: string[]; nonClaims: string[] } { const framed = manifest.schema_id === "riff://wind-turbine-maintenance/replay-manifest/framed/v1"; if (!framed) { const identityFields = "workflow_label" in manifest ? REPLAY_IDENTITY_FIELDS : LEGACY_IDENTITY_FIELDS; const expected = [...identityFields, "claim_labels", "source_artifact", "source_sha256", "canonical_event_sha256", "event_count", "frame_policy"]; exactKeys(manifest, expected, "mesa_run_corrupt"); const identity = this.#replayIdentity(run); for (const key of identityFields) if (manifest[key] !== identity[key]) throw new ApiError(500, "mesa_run_corrupt", "The legacy replay identity differs from the committed run."); const claimLabels = this.#labels(manifest.claim_labels, CLAIM_LABELS, "claim labels"); exactKeys(manifest.frame_policy, ["kind", "full_event_log_retained"], "mesa_run_corrupt"); if (manifest.source_artifact !== "domain-events.jsonl" || manifest.source_sha256 !== eventDeclaration.sha256 || !HEX.test(String(manifest.canonical_event_sha256)) || !Number.isSafeInteger(manifest.event_count) || Number(manifest.event_count) < 1 || (manifest.frame_policy as Plain).kind !== "daily_projection" || (manifest.frame_policy as Plain).full_event_log_retained !== true) throw new ApiError(500, "mesa_run_corrupt", "The legacy replay manifest is invalid."); return { kind: "legacy_frameless", frames: [], ranges: [], claimLabels, nonClaims: [...NON_CLAIMS] }; }
    const kind = manifest.manifest_kind; const common = ["schema_id", "schema_version", "canonical_json_version", "manifest_kind", "identity", "generator_version", "sampling_algorithm", "declared_population", "event_source", "sample_days", "sample_days_sha256", "frame_count", "source_event_ranges", "frames", "claim_labels", "non_claims", ...(kind === "unavailable_population_limit" ? ["unavailable_reason"] : [])]; exactKeys(manifest, common, "mesa_run_corrupt"); if (manifest.schema_id !== "riff://wind-turbine-maintenance/replay-manifest/framed/v1" || !["complete", "unavailable_population_limit"].includes(String(kind)) || manifest.schema_version !== 1 || manifest.canonical_json_version !== GATE3_CANONICAL || manifest.generator_version !== "wind-worker-sampled-replay-v1" || manifest.sampling_algorithm !== "wind-replay-sample-days-v1") throw new ApiError(500, "mesa_run_corrupt", "The framed replay discriminator is invalid."); this.#assertArtifactIdentity(manifest, run, true); const claimLabels = this.#labels(manifest.claim_labels, CLAIM_LABELS, "claim labels"); const nonClaims = this.#labels(manifest.non_claims, NON_CLAIMS, "non-claims"); exactKeys(manifest.declared_population, ["turbine_count", "crew_count"], "mesa_run_corrupt"); exactKeys(manifest.event_source, ["logical_name", "byte_length", "event_count", "raw_sha256", "semantic_sha256", "final_newline"], "mesa_run_corrupt"); const population = manifest.declared_population as Plain; const source = manifest.event_source as Plain; if (!Number.isSafeInteger(population.turbine_count) || !Number.isSafeInteger(population.crew_count) || Number(population.turbine_count) < 1 || Number(population.crew_count) < 1 || source.logical_name !== "domain-events.jsonl" || source.raw_sha256 !== eventDeclaration.sha256 || !HEX.test(String(source.semantic_sha256)) || !Number.isSafeInteger(source.byte_length) || Number(source.byte_length) < 1 || !Number.isSafeInteger(source.event_count) || Number(source.event_count) < 1 || source.final_newline !== true || !Array.isArray(manifest.sample_days) || manifest.sample_days.some((day, index, days) => !Number.isSafeInteger(day) || Number(day) < 0 || index > 0 && Number(day) <= Number(days[index - 1])) || !Array.isArray(manifest.frames) || !Array.isArray(manifest.source_event_ranges) || manifest.sample_days_sha256 !== sha256Hex(canonicalJsonV2(manifest.sample_days))) throw new ApiError(500, "mesa_run_corrupt", "The framed replay source contract is invalid.");
    const frames = manifest.frames as Plain[]; const ranges = manifest.source_event_ranges as Plain[]; if (kind === "unavailable_population_limit") { if (manifest.unavailable_reason !== "population_exceeds_frame_contract" || population.turbine_count <= 100 && population.crew_count <= 50 || manifest.frame_count !== 0 || manifest.sample_days.length || frames.length || ranges.length) throw new ApiError(500, "mesa_run_corrupt", "The unavailable replay branch is invalid."); return { kind, frames, ranges, claimLabels, nonClaims }; }
    if (population.turbine_count > 100 || population.crew_count > 50 || manifest.frame_count !== frames.length || frames.length !== ranges.length || frames.length !== manifest.sample_days.length || frames.length < 1 || frames.length > 120) throw new ApiError(500, "mesa_run_corrupt", "The complete replay population/frame contract is invalid."); let nextOffset = 0; let nextSequence = 1; for (let index = 0; index < frames.length; index += 1) { const frame = frames[index]; const range = ranges[index]; exactKeys(range, ["range_index", "event_count", "first_sequence", "last_sequence", "byte_offset", "byte_length", "raw_range_sha256", "semantic_range_sha256"], "mesa_run_corrupt"); exactKeys(frame, ["frame_index", "identity", "day", "phase", "through_event_sequence", "source_event_range_index", "frame_state_sha256", "depot", "turbines", "crews", "queues", "daily_metrics"], "mesa_run_corrupt"); this.#assertArtifactIdentity(frame, run, true); exactKeys(frame.depot, ["x_km", "y_km"], "mesa_run_corrupt"); exactKeys(frame.queues, ["corrective", "planned"], "mesa_run_corrupt"); this.#verifyMetricProjection(frame.daily_metrics, properties); if (frame.frame_index !== index || frame.source_event_range_index !== index || frame.day !== manifest.sample_days[index] || !["warmup", "measurement", "horizon_end"].includes(String(frame.phase)) || !/^fs_[0-9a-f]{64}$/u.test(String(frame.frame_state_sha256)) || !Array.isArray(frame.turbines) || !Array.isArray(frame.crews) || frame.turbines.length !== population.turbine_count || frame.crews.length !== population.crew_count || range.range_index !== index || range.first_sequence !== nextSequence || range.byte_offset !== nextOffset || !Number.isSafeInteger(range.event_count) || Number(range.event_count) < 1 || !Number.isSafeInteger(range.last_sequence) || !Number.isSafeInteger(range.byte_length) || Number(range.byte_length) < 1 || range.event_count !== Number(range.last_sequence) - Number(range.first_sequence) + 1 || frame.through_event_sequence !== range.last_sequence || !HEX.test(String(range.raw_range_sha256)) || !HEX.test(String(range.semantic_range_sha256)) || !Number.isSafeInteger((frame.queues as Plain).corrective) || !Number.isSafeInteger((frame.queues as Plain).planned) || Number((frame.queues as Plain).corrective) < 0 || Number((frame.queues as Plain).planned) < 0) throw new ApiError(500, "mesa_run_corrupt", "A replay frame/range is invalid."); const turbineIds: string[] = []; for (const item of frame.turbines as unknown[]) { exactKeys(item, ["turbine_id", "x_km", "y_km", "state"], "mesa_run_corrupt"); if (!/^turbine-[0-9]{4}$/u.test(String(item.turbine_id))) throw new ApiError(500, "mesa_run_corrupt", "A replay turbine ID is invalid."); turbineIds.push(String(item.turbine_id)); } const crewIds: string[] = []; for (const item of frame.crews as unknown[]) { exactKeys(item, ["crew_id", "x_km", "y_km", "state", "turbine_id", "work_order_id"], "mesa_run_corrupt"); if (!/^crew-[0-9]{3}$/u.test(String(item.crew_id)) || item.turbine_id !== null && !/^turbine-[0-9]{4}$/u.test(String(item.turbine_id)) || item.work_order_id !== null && !/^work-[0-9]{8}$/u.test(String(item.work_order_id))) throw new ApiError(500, "mesa_run_corrupt", "A replay crew association is invalid."); crewIds.push(String(item.crew_id)); } if (turbineIds.join("\n") !== [...new Set(turbineIds)].sort().join("\n") || crewIds.join("\n") !== [...new Set(crewIds)].sort().join("\n")) throw new ApiError(500, "mesa_run_corrupt", "Replay entity IDs are not sorted unique."); nextSequence = Number(range.last_sequence) + 1; nextOffset += Number(range.byte_length); } if (nextOffset !== Number(source.byte_length) || nextSequence - 1 !== Number(source.event_count)) throw new ApiError(500, "mesa_run_corrupt", "Replay ranges do not partition the event source."); return { kind, frames, ranges, claimLabels, nonClaims }; }
  #verifyHandshake(projectId: string, value: RuntimeCandidateHandshake): void { exactKeys(value, ["schema_id", "schema_version", "canonical_json_version", "project_id", "runtime_instance_id", "actual_python_implementation", "actual_python_major_minor", "actual_mesa_version", "model_protocol_version", "candidate_source_revision", "candidate_bundle_protocol", "candidate_manifest_sha256", "candidate_file_map_sha256", "candidate_source_descriptor_digest", "active_model_revision_id", "handshake_digest"], "mesa_adapter_failure"); if (value.schema_id !== "riff://mesa-wind/runtime-candidate-handshake/v1" || value.schema_version !== 1 || value.canonical_json_version !== GATE3_CANONICAL || value.project_id !== projectId || digestWithout("rh_", value as unknown as Plain, "handshake_digest") !== value.handshake_digest) throw new ApiError(502, "mesa_adapter_failure", "The Mesa runtime handshake failed integrity verification."); if (value.actual_python_implementation !== "CPython" || value.actual_python_major_minor !== "3.12" || value.actual_mesa_version !== "3.5.1" || value.model_protocol_version !== "wind-turbine-maintenance-v2-framed-replay" || value.candidate_bundle_protocol !== "wind-turbine-maintenance-bundle-v2-framed") throw new ApiError(409, "incompatible_framed_runtime", "The Mesa runtime is incompatible with the reviewed framed model."); }
  #loadCandidateSourceDescriptor(): any {
    const mesaRoot = resolve(import.meta.dirname, "../../mesa_service/src/mesa_service"); const legacy = join(mesaRoot, "model_assets", "wind_turbine_maintenance"); const framed = join(mesaRoot, "model_assets", "wind_turbine_maintenance_framed");
    const sourcePaths: Record<string, string> = {
      "model.py": join(mesaRoot, "models", "wind_turbine_maintenance", "framed_model.py"), "model-spec.json": join(framed, "model-spec.json"), "parameter-schema.json": join(framed, "parameter-schema.json"), "execution-field-schema.json": join(framed, "execution-field-schema.json"),
      "metric-schema.json": join(legacy, "metric-schema.json"), "visualization.json": join(legacy, "visualization.json"), "traceability.json": join(legacy, "traceability.json"), "provenance.json": join(legacy, "provenance.json"),
      "defaults/source-field-service-reference.json": join(legacy, "defaults", "source-field-service-reference.json"), "defaults/wind-turbine-maintenance-demo-v1.json": join(legacy, "defaults", "wind-turbine-maintenance-demo-v1.json"), "tests/microcase.json": join(legacy, "tests", "microcase.json"), "tests/source-transition-disposition.json": join(legacy, "tests", "source-transition-disposition.json"),
    };
    if (Object.values(sourcePaths).some((path) => !existsSync(path) || lstatSync(path).isSymbolicLink() || !lstatSync(path).isFile())) throw new ApiError(404, "framed_candidate_unavailable", "The reviewed framed candidate sources are not installed.");
    const sourceBytes = Object.fromEntries(Object.entries(sourcePaths).map(([name, path]) => [name, name === "model.py" ? readFileSync(path) : Buffer.concat([canonicalJsonV2(this.#readInstalledJson(path)), Buffer.from("\n")])])) as Record<string, Buffer>; const legacyModel = readFileSync(join(mesaRoot, "models", "wind_turbine_maintenance", "model.py")); if (legacyModel.at(-1) !== 0x0a) throw new ApiError(500, "framed_candidate_unavailable", "The reviewed legacy model source is not LF-terminated."); sourceBytes["model.py"] = Buffer.concat([legacyModel, Buffer.from("\n"), sourceBytes["model.py"]]);
    const files = Object.fromEntries(Object.entries(sourceBytes).map(([name, bytes]) => [name, { sha256: sha256Hex(bytes), byte_length: bytes.byteLength, media_type: name === "model.py" ? "text/x-python" : "application/json" }]));
    const runtime_profile = { canonical_json_version: GATE3_CANONICAL, mesa_version: "3.5.1", model_protocol_version: "wind-turbine-maintenance-v2-framed-replay", python_implementation: "CPython", python_major_minor: "3.12" }; const revisionPreimage = { schema_version: 2, bundle_protocol: "wind-turbine-maintenance-bundle-v2-framed", model_id: MODEL_ID, runtime_profile, files }; const modelRevisionId = `mr_${sha256Hex(canonicalJsonV2(revisionPreimage))}`; const manifest = { schema_version: 2, bundle_protocol: "wind-turbine-maintenance-bundle-v2-framed", model_id: MODEL_ID, model_revision_id: modelRevisionId, runtime_profile, files }; const sourceCore = { candidate_source_revision: `wsrc_${sha256Hex(canonicalJsonV2({ manifest, source_contract: "gate3-reviewed-framed-v1" }))}`, candidate_bundle_protocol: "wind-turbine-maintenance-bundle-v2-framed", candidate_manifest_sha256: sha256Hex(Buffer.concat([canonicalJsonV2(manifest), Buffer.from("\n")])), candidate_file_map_sha256: sha256Hex(canonicalJsonV2(files)), candidate_model_revision_id: modelRevisionId }; const sourceDescriptorDigest = `wsrcd_${sha256Hex(canonicalJsonV2(sourceCore))}`;
    return { candidate_source_revision: sourceCore.candidate_source_revision, model_revision_id: modelRevisionId, manifest_sha256: sourceCore.candidate_manifest_sha256, file_map_sha256: sourceCore.candidate_file_map_sha256, source_descriptor_digest: sourceDescriptorDigest, preset_sha256: (files[BINDING_FILES[0]] as any).sha256, provenance_sha256: (files[BINDING_FILES[1]] as any).sha256 };
  }
  #bundleRoot(projectId: string, revisionId: string): string { const root = join(this.#workspaceRoot, "projects", projectId, "models", MODEL_ID, "revisions", revisionId); if (!existsSync(root) || lstatSync(root).isSymbolicLink() || !lstatSync(root).isDirectory()) throw new ApiError(404, "resource_not_found", "The requested resource was not found."); const resolved = realpathSync(root); const project = realpathSync(join(this.#workspaceRoot, "projects", projectId)); if (!resolved.startsWith(`${project}/`)) throw new ApiError(500, "immutable_record_corrupt", "The model bundle escapes its project."); return resolved; }
  #readBundleFile(root: string, name: string): Buffer { const path = join(root, name); if (!existsSync(path) || lstatSync(path).isSymbolicLink() || !lstatSync(path).isFile() || !realpathSync(path).startsWith(`${root}/`)) throw new ApiError(500, "immutable_record_corrupt", "A model bundle source is not a safe regular file."); const bytes = readFileSync(path); if (bytes.byteLength > 512 * 1024) throw new ApiError(500, "immutable_record_corrupt", "A model bundle source exceeds its bound."); return bytes; }
  #readJsonFile(path: string): Plain { let bytes: Buffer; try { bytes = readFileSync(path); } catch { throw new ApiError(500, "immutable_record_corrupt", "An immutable JSON record is unavailable."); } if (bytes.at(-1) !== 0x0a) throw new ApiError(500, "immutable_record_corrupt", `${path.split("/").at(-1)}: immutable record lacks final LF.`); let value: unknown; try { value = JSON.parse(bytes.subarray(0, -1).toString("utf8")); } catch { throw new ApiError(500, "immutable_record_corrupt", `${path.split("/").at(-1)}: immutable record is invalid.`); } if (!plain(value) || !canonicalJsonV2(value).equals(bytes.subarray(0, -1))) throw new ApiError(500, "immutable_record_corrupt", `${path.split("/").at(-1)}: immutable record is not exact canonical-v2.`); return value; }
  #readJsonBytes(bytes: Buffer): Plain { let value: unknown; try { value = JSON.parse(bytes.toString("utf8")); } catch { throw new ApiError(500, "immutable_record_corrupt", "A verified JSON source is invalid."); } if (!plain(value)) throw new ApiError(500, "immutable_record_corrupt", "A verified JSON source is invalid."); return value; }
  #readInstalledJson(path: string): Plain { let value: unknown; try { value = JSON.parse(readFileSync(path, "utf8")); } catch { throw new ApiError(500, "framed_candidate_unavailable", "A reviewed installed source is invalid JSON."); } if (!plain(value)) throw new ApiError(500, "framed_candidate_unavailable", "A reviewed installed source is invalid JSON."); return value; }
  #verifyFramedParameterContract(schema: Plain, preset: Plain): void {
    exactKeys(schema, ["schema_id", "schema_version", "canonical_json_version", "type", "additionalProperties", "required", "properties"], "immutable_record_corrupt");
    if (schema.schema_id !== "riff://wind-turbine-maintenance/parameters/v2" || schema.schema_version !== 2 || schema.canonical_json_version !== GATE3_CANONICAL || schema.type !== "object" || schema.additionalProperties !== false || !Array.isArray(schema.required) || schema.required.length !== 26 || !plain(schema.properties) || Object.keys(schema.properties).sort().join("\n") !== [...schema.required].sort().join("\n") || !plain(preset.parameters)) throw new ApiError(500, "immutable_record_corrupt", "The framed parameter contract is invalid.");
    if (Object.keys(preset.parameters).sort().join("\n") !== [...schema.required].sort().join("\n")) throw new ApiError(500, "immutable_record_corrupt", "The framed preset and parameter schema differ.");
    const groups = new Map<string, Record<string, number>>();
    for (const [name, raw] of Object.entries(schema.properties)) {
      if (!plain(raw) || !plain(raw.provenance)) throw new ApiError(500, "immutable_record_corrupt", "A framed parameter property is invalid."); const property = raw as Plain; const boolean = property.type === "boolean";
      const expectedKeys = boolean ? ["type", "display_name", "section_id", "display_order", "unit", "provenance", "distribution_group_id", "distribution_family", "distribution_role"] : ["type", "display_name", "section_id", "display_order", "unit", "provenance", "minimum", "maximum", "distribution_group_id", "distribution_family", "distribution_role"]; exactKeys(property, expectedKeys, "immutable_record_corrupt"); exactKeys(property.provenance, ["source_id", "source_locator", "disposition"], "immutable_record_corrupt");
      const value = preset.parameters[name]; if (boolean) { if (property.unit !== "boolean" || typeof value !== "boolean" || property.distribution_group_id !== null || property.distribution_family !== null || property.distribution_role !== null) throw new ApiError(500, "immutable_record_corrupt", "A boolean parameter must use the exact boolean schema branch."); continue; }
      if (!["integer", "number"].includes(String(property.type)) || typeof property.minimum !== "number" || typeof property.maximum !== "number" || typeof value !== "number" || !Number.isFinite(value) || property.type === "integer" && !Number.isInteger(value) || value < Number(property.minimum) || value > Number(property.maximum)) throw new ApiError(500, "immutable_record_corrupt", "A framed numeric parameter property is invalid.");
      if (property.distribution_group_id === null) { if (property.distribution_family !== null || property.distribution_role !== null) throw new ApiError(500, "immutable_record_corrupt", "A scalar parameter has partial distribution metadata."); }
      else { if (typeof property.distribution_group_id !== "string" || property.distribution_family !== "triangular" || !["low", "mode", "high"].includes(String(property.distribution_role))) throw new ApiError(500, "immutable_record_corrupt", "A triangular parameter has invalid metadata."); const group = groups.get(property.distribution_group_id) ?? {}; if (group[String(property.distribution_role)] !== undefined) throw new ApiError(500, "immutable_record_corrupt", "A triangular distribution role is duplicated."); group[String(property.distribution_role)] = value; groups.set(property.distribution_group_id, group); }
    }
    if ((schema.properties as Plain).major_replacement_enabled && ((schema.properties as Plain).major_replacement_enabled as Plain).type !== "boolean") throw new ApiError(500, "immutable_record_corrupt", "major_replacement_enabled must remain boolean.");
    for (const group of groups.values()) if (Object.keys(group).sort().join() !== "high,low,mode" || group.low > group.mode || group.mode > group.high) throw new ApiError(500, "immutable_record_corrupt", "A triangular distribution group is incomplete or unordered.");
  }
  #encodeCursor(projectId: string, subjectId: string, createdAt: string, attestationId: string): string { const body = { project_id: projectId, subject_revision_id: subjectId, created_at: createdAt, attestation_id: attestationId, schema_version: 1 }; const json = canonicalJsonV2(body); const digest = sha256Hex(json); return Buffer.from(canonicalJsonV2({ ...body, digest })).toString("base64url"); }
  #decodeCursor(cursor: string, projectId: string, subjectId: string): { created_at: string; attestation_id: string } { let value: unknown; try { value = JSON.parse(Buffer.from(cursor, "base64url").toString("utf8")); } catch { throw new ApiError(422, "invalid_request", "The attestation cursor is invalid."); } exactKeys(value, ["project_id", "subject_revision_id", "created_at", "attestation_id", "schema_version", "digest"], "invalid_request"); const { digest, ...body } = value; if (value.project_id !== projectId || value.subject_revision_id !== subjectId || value.schema_version !== 1 || digest !== sha256Hex(canonicalJsonV2(body))) throw new ApiError(422, "invalid_request", "The attestation cursor is invalid."); return { created_at: String(value.created_at), attestation_id: String(value.attestation_id) }; }
}

const numeric = (value: string): string | number => value.trim() !== "" && Number.isFinite(Number(value)) ? Number(value) : value;
const parseCsv = (text: string): string[][] => { const rows: string[][] = []; let row: string[] = []; let field = ""; let quoted = false; for (let index = 0; index < text.length; index += 1) { const character = text[index]; if (quoted) { if (character === '"' && text[index + 1] === '"') { field += '"'; index += 1; } else if (character === '"') quoted = false; else field += character; } else if (character === '"' && field === "") quoted = true; else if (character === ",") { row.push(field); field = ""; } else if (character === "\n") { row.push(field.replace(/\r$/u, "")); rows.push(row); row = []; field = ""; } else field += character; } if (quoted) throw new ApiError(500, "mesa_run_corrupt", "The KPI CSV has an unterminated quote."); if (field || row.length) { row.push(field); rows.push(row); } if (rows.length && rows.some((item) => item.length !== rows[0].length)) throw new ApiError(500, "mesa_run_corrupt", "The KPI CSV rows have inconsistent columns."); return rows; };
const matchesEvent = (event: Plain, filters: Record<string, string | number | null>): boolean => { const day = Number(event.sim_time_days); if (filters.from_day !== null && day < Number(filters.from_day) || filters.to_day !== null && day > Number(filters.to_day) || filters.event_type !== null && event.event_type !== filters.event_type) return false; const payload = plain(event.payload) ? event.payload : {}; for (const key of ["turbine_id", "crew_id", "work_order_id"]) if (filters[key] !== null && event[key] !== filters[key] && payload[key] !== filters[key]) return false; return true; };
