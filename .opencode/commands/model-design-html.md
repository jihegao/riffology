---
description: 生成模型设计 HTML 并在外部浏览器中打开
---

Use the `simulation-model-visualization` skill for this request: $ARGUMENTS
Write the standalone HTML first, then open that exact local file in the external system browser.
